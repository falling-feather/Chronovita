# Chronovita 研发路线图

> **历史路线归档**：本文保留 V0.1.x-V0.7.4 的阶段记录，不再维护当前任务状态。当前版本与总览见 [docs/00-项目总纲.md](docs/00-项目总纲.md)，未来规划与版本分配见 [docs/02-项目规划与设计总纲.md](docs/02-项目规划与设计总纲.md)，完成历史见 [docs/05-发布历史与归档.md](docs/05-发布历史与归档.md)。

---

## 整体阶段全景

| 阶段 | 版本范围 | 目标 | 状态 |
| --- | --- | --- | --- |
| 0 · 平台壳 | v0.1.x | 五模块路由 + 视觉骨架 + 占位 | ✅ 完成 |
| 1 · 课程数据 | v0.2.x | 课程/章节模型 + 14 朝代 48 节正文 + 课程中心地图 | ✅ 完成 |
| 2 · 练 · saga 引擎 | v0.3.x ~ v0.6.x | 互动小说引擎 + 全朝代 saga 模板 + 收束/防循环 | ✅ 完成 |
| 3 · 问 · 双模智者 | v0.7.x | DeepSeek v4-pro + 专家/同窗双 persona | ✅ 完成 |
| 4 · 系统化打磨 | v0.8.x | 学习进度持久化、画板自动保存、UX 与稳定性 | ⏳ 进行中 |
| 5 · 看 · 沉浸叙事 | v0.9.x | 关键词词汇卡 + 视频/AI 旁白接入 | 计划 |
| 6 · 教师后台与多用户 | v1.x | 班级、作业、错题、教师视图 | 计划 |
| 7 · 部署与扩展 | v2.x | Docker / Postgres / 向量库 / 多模型 | 计划 |

当前短/中/长期落地条目统一见 [docs/02-项目规划与设计总纲.md](docs/02-项目规划与设计总纲.md)。

---

## 优化构筑记录

> 自 v0.1.0 起重新计数。每次正式提交追加一条。新条目置顶。

- **V0.7.4 · 「同窗」自定义对象（S4）**
  - 同窗下拉新增「自定义对象…」入口：Modal 输入姓名 + 一句话简介
  - 本地缓存 5 个常用（`localStorage chronovita.custom_peers`），下次直接出现在下拉
  - 后端 `AskRequest` 扩展 `peer_intro`，注入到 peer system prompt 帮助 LLM 锁定身份与时代
  - 同名优先用课程内置；下拉按「课程内置 / 我的自定义 / 自定义入口」分组
  - 切课时若 peerCharacter 不在新候选里自动回退第一个

- **V0.7.3 · 画板自动保存（S2）**
  - `LessonCreate` 增加 debounce 800ms 自动保存：节点/边变化静默 `PUT /practice/canvas/:lid`
  - 工具栏右上 chip 替换原"保存到云端"按钮：加载中 / 保存中… / 已保存于 HH:mm / 保存失败（点击重试）
  - 错误仅 toast 一次，下次成功时复位，避免连续失败刷屏
  - 初次加载后下一帧才置 `loaded=true`，避免首次读出的种子节点触发误存

- **V0.7.2 · 学习进度持久化 + 首页/我的学习接通真实数据（S1+S3）**
  - 后端 `LessonProgress` 表 + `/learning/progress` 读写接口
  - 前端 `LessonShell` 自动 touch，HomePage / LearningPage 取真实进度

- **V0.7.1 · 文档体系刷新到 V0.7.0 + 新增 Planning 短中长期规划**

- **V0.7.0 · 「问」板块接入 deepseek-v4-pro + 同窗模式真实历史人物代入**
  - ask 路由切换到 `deepseek-v4-pro`（准确度优先），与 saga 的 `v4-flash` 并存
  - 重写 expert/peer system prompt：专家=学界共识 + 末尾延伸阅读；同窗=第一人称 + 不预言后世硬约束
  - 前端 `LessonAsk` 新增「同窗对象」下拉，候选自 `lesson.figures`，模式/对象切换自动重置欢迎语
  - 修复 SSE chunk 重复 bug：StrictMode 下 setState 函数被双调用，原代码 mutate 同一消息对象致每个汉字写两次

- **V0.6.0 ~ V0.6.3 · saga 收束触发器 + 选项一致性修复**
  - V0.6.0：saga 引擎收束触发器 + persona 点名 + 防循环规则
  - V0.6.1：压力测脚本默认 7 轮 + 跨朝代验收（86% 持率均一致性、L102 三轮收束）
  - V0.6.2：修复 saga 选项栏与剧情脱节 bug
  - V0.6.3：saga 选项缺失时强制清空避免陈旧残留

- **V0.5.0 · saga 全本朝代覆盖**
  - 48 个 saga 模板，覆盖宋元明清晚清等 9 个朝代全部 lesson 互动剧本
  - V0.5.1：saga 压力测脚本（多轮验证 ended/记忆压缩/人物一致）

- **V0.4.0 · saga 模板补齐 + 魏晋 ID 冲突修复**
  - 修复魏晋 ID 冲突（L501-L504 → L1501-L1504）
  - 魏晋 4 课 saga（赤壁/嵇康/孝文帝/鸠摩罗什），saga 模板累计 21
  - V0.4.1：补齐至 30 个（先秦思想/秦统一/隋唐补遗 9 课）

- **V0.3.0 · 先秦练问创板块实装 + saga 引擎 + 地图修复**
  - V0.3.1：saga 批量验证脚本 + L102-L105 全部通过（4/4）
  - V0.3.2：秦汉 6 课 saga 模板（L401-L403 一统肇基 + L501-L503 思想科技）
  - V0.3.3：隋唐 6 课 saga 模板（L601/L602 制度 + L701/L703 政治 + L801/L802 文化）

- **V0.2.0 ~ V0.2.9 · 课程体系初建 + 课程中心地图**
  - V0.2.0：初课程体系与教材级课程内容（14 朝代 48 节正文，详见 services/courses/__init__.py）
  - V0.2.1：新增魏晋南北朝时代与课程充填
  - V0.2.2 ~ V0.2.9：课程中心地图 — 入场 + 历史路径 + 子时段切换 + 都城联动课程过滤 + 朝代版图随时代变化（设计文档 [docs/Map-Design.md](docs/Map-Design.md)）

- **v0.1.0 · 平台五模块壳层落地**
  - 顶栏 + 5 路由 + 五模块占位页（首页 / 课程中心 / 我的学习 / 实践课堂 / 个人中心）
  - 课程中心三栏布局（CSS Grid）+ Bilibili iframe 嵌入
  - `muban/` 模板库（PageShell / ThreeColumnLayout / StatTabsList / FilterCardGrid / SidebarFormPanel）
  - ADR 精简至四份：技术栈选型 / 后端持久化 / LLM 适配层 / 模板规范
