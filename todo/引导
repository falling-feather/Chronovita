你说得完全对，是我刚才偷懒了。既然是给编程智能体（尤其是像Cursor这种基于Agentic workflow的AI）下达初始构建指令，如果没有把我们的核心业务逻辑（“看-练-问-创”闭环、DAG算法推演、双模态智能体）喂给它，它写出来的文档必定是空洞的模板代码。

并且，既然是面向你未来的代码产出，还需要把你一直以来的代码习惯（不加注释、保持代码风格）一并写进它的全局指令里。

这是为你重新整理的、可以直接“一键复制”给编程智能体的终极指令（Prompt）。它既包含了你提的4点硬性工程要求，也融合了我们整个项目的业务架构：

请将以下指令严格写入你的全局记忆（Global Memory），并在未来的所有开发和交互中绝对遵循：

【全局规范与记忆】

Git版本管理原则：微小的修复无需提交。标准提交必须严格遵循“v大版本.中版本.小版本 - 中文的版本更新规划”格式（例如：v1.0.0 - 项目构建完成，正式运行提交）。每迭代一个中等版本时，必须合并所有小版本作为分支进行构筑。确保Git树中最终仅显示中版本和大版本的节点。

代码风格准则：在后续所有的代码输出中，绝对不要在代码中编写任何注释（Do not write comments in the code），并严格保持当前框架的标准代码风格。

语言与排版规范：本项目主体围绕中国历史展开，前端UI内容必须严格避免中英文结合的情况（No Chinglish），保持纯中文的沉浸式历史主题排版。

【项目背景与业务架构】
本项目名称为 Chronovita（远端仓库：https://github.com/falling-feather/Chronovita ）。
这是一个面向历史推演的“未来课堂”虚拟仿真教学平台。核心业务包含四个闭环模块：

看（仿真微视频）：基于大模型特征约束生成的高保真历史情境。

练（沙盘推演）：基于DAG（有向无环图）与状态压缩DP算法构建的历史因果推演引擎，允许用户输入变量改变历史走向。

问（双模态智能体）：结合RAG检索校验的双模态智能体（支持情境NPC与领域专家无缝切换）。

创（知识谱系）：基于用户交互数据生成的低代码知识谱系画布。

【当前执行任务】
请立刻执行以下两项初始化任务：

任务一：文档初始化
请围绕上述项目架构，在项目根目录编写三个核心Markdown文档：

Development_Spec.md（项目开发文档）：必须绝对详细透彻地记录项目内容，包含文档分级结构、前后端接口逻辑定义、核心待实现功能模块，以及短期项目研发规划。

Roadmap.md（项目规划文档）：需覆盖项目的短期、中期、长期开发计划，并专门设立一个“优化构筑记录（Optimization & Refinement Log）”区块，要求随着未来开发进度不断将新的优化策略补充进去。

README.md（项目介绍文档）：简洁清晰地描述Chronovita的项目愿景、架构构建方式以及核心业务的交互逻辑。

任务二：UI设计初始化
请直接调度 pencil 的 MCP（Model Context Protocol）服务，在项目中自行创建对应上述四个核心业务模块的几个 .pen 后缀的设计文件，准备接收我后续提供的布局范例进行细化的UI设计。