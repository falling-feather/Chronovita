# Chronovita - 07-课程内容历史库运维指南

> **文档梗概**：记录独立私有课程内容历史库的真实身份、教师投稿边界、校验门禁、凭据策略、维护方式与故障恢复原则。
>
> **当前基线**：**V0.9.38 候选**。私有仓库治理、确定性归档、持久状态、GitHub PR 后端链路、教师发布界面和无密钥 Windows 教师包已经建立；`v0.9.37` 教师包与校验和已发布。V0.9.38 独立审查修复等待远端门禁与新标签包体复验；GitHub App、强制分支门禁和生产部署加固尚未完成。
>
> **具体规划法则**：当前实现以 [01-开发者文档](01-开发者文档.md) 为准；任务状态与版本安排以 [02-项目规划与设计总纲](02-项目规划与设计总纲.md) 为准；归档与 Git 发布架构决策见 [ADR-0017](adr/ADR-0017-课程内容历史库与Git发布边界.md)。
>
> **最后一次更新时间**：2026-07-25
>
> **更新者**：运维组 / 核心开发组

---

## 目录

[1. 仓库身份](#1-仓库身份) · [2. 职责边界](#2-职责边界) · [3. 教师操作](#3-教师操作边界) · [4. 归档目录](#4-归档目录) · [5. 自动校验](#5-自动校验) · [6. 凭据](#6-凭据策略) · [7. 维护恢复](#7-维护与故障恢复) · [8. 后续交付](#8-后续交付)

---

## 1. 仓库身份

| 项 | 当前值 |
| --- | --- |
| 仓库 | [`falling-feather/Chronovita-Course-Content`](https://github.com/falling-feather/Chronovita-Course-Content) |
| 可见性 | Private |
| GitHub 数字仓库 ID | `1311692460` |
| 默认分支 | `main` |
| 归档根目录 | `courses/` |
| 默认发布模式 | `pull_request` |
| 管理员模式 | `direct_commit`，必须由服务端权限和二次确认共同放行 |
| 治理基线 | `559974aa66c2c48400d57a0b8f289744dbb641bb` |
| 当前 `main` 头 | `8c4b203138187ef802323c45e421161cd97ad86d` |
| 首轮校验 | [Actions 30146239852](https://github.com/falling-feather/Chronovita-Course-Content/actions/runs/30146239852)，成功 |

源码仓库中的 [目标配置](../infra/content-history-target.json) 只记录公开仓库元数据和启用状态，不含 token、GitHub App 私钥或 installation token。后端必须复核数字仓库 ID，不能只信任可改名的 `owner/repository`。

## 2. 职责边界

内容历史库只保存已封存课程的不可变归档和 Git 审校历史：

```text
Chronovita 数据库
  草稿、账号、审校流、发布任务状态
        |
        | 精确 sealed/published release
        v
Chronovita 归档器
  内容层 + 格式层 + 教师稿 + 预览 + 仿真素材 + 清单
        |
        | 默认 Pull Request
        v
私有内容历史库
  不可变版本、差异审校、人工批准、异地留痕
```

它不是运行时数据库，不保存浏览器临时状态，不决定学生当前看到哪个版本。学生读取仍由 Chronovita 应用侧 active release pointer 控制。

## 3. 教师操作边界

V0.9.31 已在 `/admin/content` 接通教师发布界面。具备 `content.publish` 权限的教师或管理员按以下流程操作：

1. 在编辑器选择一份已经封存或发布的课程版本。
2. 预览系统生成的中文文件名、课程包清单和更新说明。
3. 点击“提交审核”，无需填写仓库、分支、路径或 token。
4. 刷新页面后继续查看任务状态；成功时获得审核链接。
5. 只有管理员在二次确认后才能使用直接提交。

普通教师不需要安装 Git、GitHub CLI，也不需要理解 branch、commit、remote 等术语。课程包下载只生成本地 ZIP，不代表已经发布到历史库；只有任务状态显示“已提交内容审核”或“已保存到课程历史库”并给出结果入口，才说明远端动作完成。若服务端未启用远端发布，面板仍允许预览和下载，并明确提示由管理员完成服务配置。

## 4. 归档目录

每个归档使用不可覆盖的内容寻址目录：

```text
courses/<course_id>/releases/<release_id>/<archive_id>/
├── 课程归档清单.json
├── 课程发布清单.json
└── lessons/<lesson_id>/
    ├── <课程标题>.json
    ├── <课程标题>-运行包.json
    ├── <课程标题>-格式层.json
    ├── <课程标题>-教师稿.md
    ├── <课程标题>-预览.html
    └── scenarios/<scenario_id>-vNNN.json
```

同一目录进入 `main` 后不得原位修改。课程内容、渲染器或任一文件字节变化时，系统必须生成新的归档身份。

## 5. 自动校验

内容库的 `Validate Chronovita content` 工作流在 push、Pull Request 和手工触发时运行：

- 复核仓库策略文件及 vendored `course-archive/v1` Schema 哈希；
- 验证归档清单 Schema、归档 checksum、清单 checksum 与派生目录；
- 拒绝父级逃逸、非 NFKC、Windows 保留名、大小写或 Unicode 碰撞；
- 逐文件核对字节数、SHA-256、UTF-8 和 JSON 可解析性；
- 拒绝清单未登记文件、符号链接、超大文件或超大归档；
- 运行正常归档、字节篡改、游离文件和凭据字段四类单元测试。

维护者可在内容库根目录运行：

```powershell
python -m pip install -r .chronovita/validation-requirements.txt
python -m unittest discover -s tests -v
python scripts/validate_content_archive.py --repository .
```

## 6. 凭据策略

生产优先使用仅安装到内容历史库的 GitHub App；过渡期可使用最小权限 fine-grained token。无论采用哪种方式：

- 凭据只能由后端环境或秘密管理系统提供；
- 浏览器请求只发送 `binding_id` 和精确归档身份；
- 仓库、课程包、教师下载包、日志和审计正文不得包含凭据；
- installation token 按需生成和刷新，不写入数据库；
- GitHub App 尚未注册或安装，不能把“仓库已创建”描述成“生产认证已完成”。

V0.9.30 起沿用的配置项：

| 环境变量 | 默认值 | 说明 |
| --- | --- | --- |
| `CHRONO_CONTENT_HISTORY_TARGET_PATH` | `infra/content-history-target.json` | 只含公开目标元数据，不含凭据 |
| `CHRONO_GITHUB_PUBLICATION_ENABLED` | `false` | 未完成配置时保持关闭，不影响本地编辑和 ZIP 下载 |
| `CHRONO_GITHUB_PUBLICATION_TOKEN` | 空 | 仅由服务端环境注入的细粒度 token，Settings 中使用 `SecretStr` |
| `CHRONO_GITHUB_API_BASE_URL` | `https://api.github.com` | 当前只接受该官方地址；其他主机、端口或路径在发送凭据前失败关闭 |
| `CHRONO_GITHUB_TIMEOUT_SECONDS` | `20` | 单次 GitHub 请求上限 |
| `CHRONO_GITHUB_PUBLICATION_STALE_SECONDS` | `600` | 后续状态机判断中断任务可恢复的时间窗 |

checked-in [目标配置](../infra/content-history-target.json) 中的 `runtime_publication_enabled=true` 只表示后端适配器已通过验收；环境变量默认仍为 `false`。启用前应为后端进程注入仅授权该私有内容库的 token，并重启 API。教师浏览器、课程 ZIP 和一键分发包都不得获得该 token。

### 6.1 Windows 教师包凭据

V0.9.32 教师包不预置 token。项目管理员向教师单独发放仅授权内容历史库的细粒度 token 后，教师执行：

```text
scripts\configure-content-history.cmd
```

输入不回显。脚本使用 Windows DPAPI 把密文写入 `.chronovita-local/content-history-token.dpapi`，该文件只能由同一台电脑上的同一 Windows 账号解密；更换电脑或账号必须重新配置。启动器在创建 API 子进程前解密并注入 `CHRONO_GITHUB_PUBLICATION_TOKEN`，随后立即清除父进程变量，再启动 Vite 生产预览服务，因此浏览器和前端进程不继承凭据。删除凭据可执行：

```text
scripts\configure-content-history.cmd -Remove
```

凭据配置变化后，应先执行 `scripts\stop-teacher-editor.cmd`，再重新启动编辑器。DPAPI 只解决教师本机静态存储，不能替代 GitHub App 的最小权限、轮换和集中撤销能力；GitHub App 仍是生产目标。

教师启动器使用 `apps/api/requirements.lock` 与 `pip --require-hashes` 安装全部传递依赖，使用 `apps/web/package-lock.json` 与 `npm ci` 安装前端依赖；两个锁文件都进入发行 manifest。端口已有 Chronovita 时还会核对 `/healthz` 应用版本，版本不同就要求先停止旧服务，不会直接打开旧页面。

标签 Release 不是任意提交的下载通道。`Teacher release` 工作流只接受从源码仓库 `main` 或 `class` 可达的提交，并要求该完整 SHA 的 Backend quality、PostgreSQL service、Secret history 和 Windows launcher 四路 push 检查都成功；Windows runner 还会用 PowerShell 5.1 完成 DPAPI 回环、冷启动、React 渲染、明文凭据落盘扫描和停止脚本验收后才发布 prerelease。维护者可先在 GitHub 页面创建同标签的空 prerelease；流水线会在全部验收通过后更新说明并补传 ZIP 与 `.sha256`，重复执行会覆盖同名资产而不会产生第二个 Release。

首个已验收下载入口是 [v0.9.37 prerelease](https://github.com/falling-feather/Chronovita/releases/tag/v0.9.37)。标签流水线 [30159636006](https://github.com/falling-feather/Chronovita/actions/runs/30159636006) 发布 `Chronovita-Teacher-Editor-v0.9.37-windows.zip` 和同名 `.sha256`；ZIP SHA-256 为 `40bb46496727a101c3257ed20950e820a8e1ce3ca0c2f06eea880cbdf827b518`。

V0.9.30 的真实验收记录为 [内容 PR #2](https://github.com/falling-feather/Chronovita-Course-Content/pull/2)、源 Commit `57dfb20`、合并提交 `8c4b203`、[PR 校验 30147300184](https://github.com/falling-feather/Chronovita-Course-Content/actions/runs/30147300184) 和 [main 校验 30147312831](https://github.com/falling-feather/Chronovita-Course-Content/actions/runs/30147312831)。首条归档是大禹技术夹具，用于证明完整链路，不代表教师正式内容审校结论。

V0.9.31 的教师界面验收记录为 [内容 PR #3](https://github.com/falling-feather/Chronovita-Course-Content/pull/3) 和 [校验 30148467866](https://github.com/falling-feather/Chronovita-Course-Content/actions/runs/30148467866)。该 PR 来自隔离临时课程，只验证“教师界面 -> 后端状态机 -> 私有仓库 PR -> Actions”链路，不属于正式课程内容；V0.9.38 已补充审计说明后将其关闭且未合并，并删除源分支。

## 7. 维护与故障恢复

- 内容 PR 校验失败时先修复生成器或源内容，再生成新归档；不要手改清单哈希。
- Git Commit 已创建而 PR 创建超时时，后端应保留 Commit SHA 并协调恢复，不能重复造包。
- 基准分支推进产生冲突时重新读取基准 Tree；禁止 force push 覆盖历史。
- 删除或移动既有归档必须经过单独的数据治理决策，普通发布流程只新增。
- 仓库改名或转移所有者后，必须通过数字仓库 ID 复核绑定并更新文档，不能静默跟随名称。

## 8. 后续交付

| 任务 | 交付 |
| --- | --- |
| BE-006 / V0.9.29-V0.9.30 | 已完成确定性归档、ZIP、持久状态、GitHub PR/直提适配器、管理员 API 和真实 PR 验收 |
| FE-004 / V0.9.31 | 已完成教师发布预览、中文自动命名、说明、默认审核、高级直接提交、进度恢复、重试和结果链接 |
| OPS-003 / V0.9.32-V0.9.37 | 已完成教师包、受控 manifest、DPAPI 本机凭据、标签 Release 流水线、本机冷启动、五项发布兼容修复和首个远端 ZIP/`.sha256` 发布 |
| QA-002 / V0.9.38 | 已完成 mock、真实私有仓库、工程 PR 清理和独立 review 修复；待 V0.9.38 四路门禁、Windows 冷启动与 Release 资产复验 |
