# 01-开发者文档

> 面向前后端开发、后续接手者和技术负责人。本文是开发团队的全局入口：先说明 Chronovita 的项目设计路线，再说明技术路径、模块边界、代码入口、数据流、LLM 适配和后续扩展方式。更完整的产品规划见 [02-项目规划与设计总纲](02-项目规划与设计总纲.md)，内容生产规范见 [03-内容设计工作手册](03-内容设计工作手册.md)。

当前基线：**V0.9.23 已在 V0.9.22 持久化可靠性基线上建立 PostgreSQL 引导、迁移和 API 运行三层账号边界，并通过真实 PostgreSQL 16 最小权限服务矩阵与后端全量质量门禁；V0.8.x 运行闭环保持为回归基线**。

当前后端开发阶段：**V0.9.5 已完成账号、会话、内容角色矩阵、学生资源所有权与显式旧数据映射。V0.9.7—V0.9.10 完成 [ADR-0016](adr/ADR-0016-数据库版本迁移与备份恢复.md) 的连续 schema 账本、严格旧库认领、SQLite 可验证备份恢复、脱敏数据库 URL/Psycopg 方言边界和单一建表入口，DATA-001 已关闭。V0.9.11—V0.9.13 建立 production 跨字段预检、live/ready、统一请求 ID、脱敏 JSON 访问事件、生产异常收束、精确 Host 白名单、登录令牌桶和隔离的一键本地档。V0.9.14 增加 PostgreSQL 连接池/连接超时边界、生产 `verify-full` 门禁、环境变量式迁移命令和 PostgreSQL 16 服务工作流；V0.9.15 使用审计头行锁串行化跨实例事件写入，并让审计链、schema 只读复验和迁移事务在 PostgreSQL 上使用一致快照；V0.9.16 把首次管理员创建、账号创建与角色/启用状态修改放进同一个数据库级身份事务；V0.9.17 把 KV 写入改为原子 upsert，并让 CAS 在数据库锁内按规范 JSON 值和独立 presence 位比较；V0.9.18 把 PostgreSQL 迁移锁改为可配置的有界等待，并增加覆盖依赖、编译、项目文档和后端全量测试的独立质量门禁；V0.9.19 升级 FastAPI/Starlette 并把 `pip-audit` 变成跨平台质量步骤；V0.9.20 收紧遗留 Practice 身份与模型资源边界；V0.9.21 把 Cookie 与 Bearer 分开签发并绑定令牌命名空间，拒绝任意双凭据，建立闲置过期、绝对寿命、数据库 CAS 轮换、全会话吊销和事务审计；V0.9.22 为 SQLite schema/审计复验显式建立单一读事务，并让备份、恢复和失败回滚在发布目录项后执行跨平台目录同步；V0.9.23 在独立引导步骤动态创建随机口令的非超级用户迁移/运行角色，正式测试进程不再接收管理员 URL；运行角色按表获得最小 ACL，迁移账本只读、审计事件只可读取与追加，并用 DDL、受保护 DML 和角色切换反例证明 API 不依赖超级用户。独立 review 仍确认时间列、跨实例共享限流、真实 TLS/故障转移、PostgreSQL 原生备份和分支保护需要继续实施，因此当前仍不能描述为生产部署完成。任务见 [项目规划第 10 节](02-项目规划与设计总纲.md#10-后端开发推进计划)**。

最后更新：2026-07-25（核心开发组 / OPS）

## 1. 开发团队先看什么

| 目标 | 优先阅读 |
| --- | --- |
| 快速理解项目 | 本文第 2-4 节，再看 [02-项目规划与设计总纲](02-项目规划与设计总纲.md) |
| 改前端页面 | 本文第 7 节 |
| 改后端接口 | 本文第 8-10 节 |
| 改 LLM、prompt、mock 回退 | 本文第 12 节与 `services/llm/` |
| 改课程正文、人物、saga 素材 | [03-内容设计工作手册](03-内容设计工作手册.md) 与 `services/courses/`、`services/saga/` |
| 查版本状态 | [05-发布历史与归档](05-发布历史与归档.md) |

## 2. 项目设计路线速读

Chronovita 不是普通 AI 问答网站，也不是只放视频和地图的资料库。它的设计路线是把一节历史课拆成四个可交互学习动作：

```text
看：地图、课文、视频、关键词，建立历史图景
练：saga / sandbox，把历史情境变成可推演的抉择局
问：专家 / 历史人物双模态智能体，解释疑惑与时代视角
创：知识画板和智能笔记，把学习结果沉淀为个人知识谱系
```

开发时要始终保留这个闭环。新增功能如果无法归入“看、练、问、创”中的某一层，通常应先回到产品规划讨论，而不是直接加页面。

## 3. 技术路径

当前项目采用开发期单体架构，优先保证本地可演示、可快速迭代、可逐步迁移：

```text
React + Vite + TypeScript + Ant Design 前端
        |
FastAPI + Pydantic 后端
        |
services 业务服务层
        |
SQLite KV 持久化 + 文件化课程素材 + LLM 适配层
```

中长期演进路径：

| 阶段 | 技术方向 |
| --- | --- |
| 当前 | SQLite、mock/DeepSeek/OpenAI 兼容 LLM、本地课程数据、单用户或轻量演示 |
| 近期 | 管理员内容编辑器、课程文件封存、内容审校状态、关键路径测试 |
| 中期 | Postgres、对象存储或 Git 化内容仓库、教师后台、班级与作业 |
| 长期 | 向量库/RAG、内容版本审计、多模型路由、生产部署与权限治理 |

## 4. 管理员内容生产与发布

当前内容生产是具备真实账号、角色动作和审校分工的文件驱动后台，但还不是包含组织、课程分配与协作转交的完整多租户 CMS。`LessonContentPackage.status` 只描述 `draft|sealed` 制品状态；审校和学生可见性由独立工作流与课程发布清单决定。**封存不等于发布**。

```text
课程草稿 -> 校验 -> 审校 -> 课程封存 ----+
关卡表单 -> 校验 -> 关卡封存 ------------+-> 选择保留/更新/清空绑定 -> 发布 V2 整课快照
                                                                          |
学生课程 API + 正式游戏目录 <- 同一活动指针 <- 同一课程发布清单 <---------+
                                              ^
                                              └---------- 回滚生成新快照
```

| 层 | 当前实现 |
| --- | --- |
| 身份内核 | `CHRONO_AUTH_MODE=accounts` 启用真实账号；密码使用带随机盐和参数版本的 scrypt 摘要，数据库只存不透明令牌的 SHA-256 摘要。`POST /auth/login` 只签发 `HttpOnly + SameSite=Lax` Cookie 且响应正文不含令牌；`POST /auth/token` 只返回 Bearer。两类令牌使用不同命名空间，跨通道复用或同时提交 Cookie/Bearer 均失败关闭；闲置、固定到期或绝对寿命任一到达即失效，轮换后旧令牌立即无效 |
| 内容动作权限 | `/api/v1/admin/content` 全部路由通过统一 `Principal` 判断显式动作权限：模板、草稿、工作流、封存件与制品读取使用 `content.read`；课程/关卡草稿、人物与关键词写入使用 `content.author`；通过或退回使用 `content.review`；封存、运行制品登记、发布、旧版迁移与回滚使用 `content.publish`。学生角色无管理工作区权限 |
| 作者与审校边界 | accounts 模式把课程草稿首位保存者和关卡 `created_by` 绑定为稳定 `user_id`；非管理员只能修改自己的草稿，教师与审校双角色也不能审校自己参与保存的课程。首建所有权检查与写入位于同一跨线程/进程工作流锁内，避免并发抢占；管理员保留显式应急覆盖能力 |
| 审计与防伪 | 请求体中的旧 `actor`、`sealed_by` 只作兼容输入并被忽略；工作流成功事件记录真实主体，账号审计链先写入带 session、角色、请求 ID 和客户端摘要的授权事件，审计不可用时业务写入不开始。PostgreSQL 通过锁定唯一审计头行串行化跨实例追加，链复验使用一致快照，不把正常并发误判为篡改。直接上传的 sealed 关卡会由服务端替换 `sealed_by` 并重新计算 checksum。Cookie 写请求还必须携带 `CHRONO_CORS_ORIGINS` 内的可信 `Origin` |
| 本地兼容 | 一键启动继续使用显式 `legacy-local`、回环随机 token 与合成管理员主体，不写入冒充真实账号的认证审计；Bearer 与 `X-Admin-Token` 均可用。该模式仅用于单机编辑，不是生产权限模式 |
| 编辑制品 | `LessonContentPackage` 禁止未知字段，保存到 `content/drafts/{lesson_id}.json`；重复保存相同正文不会撤销已有审校状态，实质内容变化会清除旧校验与批准 |
| 最低校验 | `validate_package()` 阻止空正文、无关键词释义、无人物摘要、无 facts 或无可追溯资料的内容进入审校；AI persona、问答点、地图点和互动目标当前为警告 |
| 审校记录 | `content/workflows/{lesson_id}.json` 保存状态、revision、校验报告和连续事件历史；保存、校验、提交、退回、通过、封存、发布和回滚均留操作者与时间 |
| 封存制品 | `content/sealed/{lesson_id}-vNNN.json` 不可覆盖，记录版本、时间、操作者和 SHA-256；只有 `approved` 的当前草稿可以封存 |
| V1 制品 | 发布时通过 `course_package_from_legacy()` 生成 `content/packages/v1/{lesson_id}-vNNN.json` 兼容制品；V2 再把课程与精确 `scenario_refs` 绑定为 `content/runtime/v1/course-packages/.../vNNN-{checksum}.json` |
| 关卡暂存 | `POST /api/v1/admin/content/runtime-scenarios` 只接受 checksum 正确的 sealed `ScenarioTemplateV1`，按内容寻址写入 `content/runtime/v1/scenarios/`；暂存文件存在不表示已发布 |
| 关卡作者草稿 | `services/content/scenario_authoring.py` 使用 `scenario-author-draft/v1` 保存教师表单数据到 `content/scenario-drafts/{scenario_id}.json`；草稿允许存在尚未通过跨引用校验的规则，revision 乐观锁拒绝陈旧覆盖 |
| 关卡校验与封存 | 校验把作者草稿编译为严格 `ScenarioTemplateV1` 并返回逐字段问题；封存由服务端生成版本、认证操作者、时间与 checksum，再复用既有内容寻址暂存层；相同语义内容重复封存幂等返回原版本 |
| 教师联合发布 | `/admin/content` 的课程“审校与发布”区显示当前线上关卡，并提供“保留现状 / 更新绑定 / 清空绑定”三态；更新时从当前课程和课时的 sealed 关卡中选择精确版本，同一关卡 ID 只能选一个版本，非空绑定必须选择唯一主关卡 |
| 发布清单 | `content/releases/manifests/{course_id}/{release_id}.json` 是不可变整课快照；`content/releases/active/{course_id}.json` 是原子替换的活动指针，也是学生可见性的唯一提交点 |
| 发布事务 | `content/releases/transactions/{course_id}.json` 在指针切换前记录旧/新工作流投影；API 启动时依据活动指针自动完成或回滚，成功后立即删除 |
| 学生读取 | `services/courses` 读取 active V1/V2 清单指定的课程包，V2 课时公开最小 `scenario_refs` 与唯一 `primary_scenario_id`；`services/game_runtime` 从同一 active V2 清单读取 `audience=published` 关卡，正式启动必须携带课时响应形成的完整 release pin；校验失败时 API 返回稳定 `503`，不回退到未发布内容 |
| 回滚 | 回滚不改写历史文件，而是先复验目标课程包、全部关卡及 `RuntimeBundleV1`，再复制为新的 V2 release 并移动活动指针；不同课程互不连带回滚 |

账号模式的会话入口必须按使用方选择，不能把浏览器登录当成通用令牌接口：

| 方法与路径 | 认证通道 | 结果 |
| --- | --- | --- |
| `POST /api/v1/auth/login` | Cookie | 生产档要求可信 `Origin`；响应只设置 HttpOnly Cookie，JSON 不返回令牌 |
| `POST /api/v1/auth/token` | Bearer | JSON 返回 Bearer；若请求带旧 Cookie，响应同时清除该 Cookie，避免后续双凭据 |
| `POST /api/v1/auth/refresh` | 沿用当前通道 | 原子替换令牌；Cookie 写请求要求可信 `Origin`；旧令牌立即失效 |
| `POST /api/v1/auth/logout` | Cookie 或 Bearer | 有效会话被撤销；失效或过期 Cookie 在可信 `Origin` 下也返回 204 并清除浏览器 Cookie |
| `POST /api/v1/auth/sessions/revoke-all` | 当前真实账号 | 递增本人 `auth_version` 并撤销全部终端会话 |
| `POST /api/v1/auth/users/{user_id}/sessions/revoke` | `auth.manage_users` | 管理员撤销目标账号全部终端会话 |

`CHRONO_AUTH_SESSION_IDLE_TIMEOUT_SECONDS <= CHRONO_AUTH_SESSION_TTL_SECONDS <= CHRONO_AUTH_SESSION_ABSOLUTE_TTL_SECONDS` 是启动硬约束，示例值依次为 7200、28800、604800 秒。V0.9.21 首次部署会拒绝 V0.9.20 及更早生成的无 `cvs_cookie_` / `cvs_bearer_` 前缀会话，已有用户需要重新登录；该失效是预期的通道隔离升级，不需要修改数据库 schema。

关键管理接口：

| 方法与路径 | 前置状态 | 结果 |
| --- | --- | --- |
| `POST /api/v1/admin/content/drafts` | 任意可校验草稿 | 原子保存草稿，建立或刷新工作流 |
| `POST /drafts/{lesson_id}/validate` | 已保存草稿 | 只写入最低内容校验报告，不改变任何学生可见内容或 release |
| `POST /drafts/{lesson_id}/submit-review` | `validated|changes_requested` | 进入 `in_review` |
| `POST /drafts/{lesson_id}/review` | `in_review` | `approve` 进入 `approved`；`changes_requested` 必须附退回说明 |
| `POST /drafts/{lesson_id}/seal` | `approved` | 生成不可变 sealed 版本，学生端仍保持原发布内容 |
| `GET /runtime-scenarios` | 管理员 | 列出已登记的 sealed 关卡及其精确版本、checksum 和内容寻址描述符 |
| `GET /runtime-scenarios/{scenario_id}/versions/{scenario_version}` | 管理员；查询参数提供 `course_id + lesson_id + scenario_checksum` | 按完整制品身份复验并返回 sealed `ScenarioTemplateV1` 与描述符，供刷新后预览和 1:1 导出；不采用“同 ID 最新版本”回退 |
| `GET /runtime-scenarios/{scenario_id}/versions/{scenario_version}/file` | 管理员；查询参数同上 | 复验完整制品身份与 checksum 后直接返回不可变封存文件的原始字节；前端不得重新序列化后冒充 1:1 导出 |
| `POST /runtime-scenarios` | sealed 且 checksum 正确的 `ScenarioTemplateV1` | 登记不可变关卡制品，不改变学生或游戏目录可见性 |
| `GET /scenario-drafts/template` | 管理员 | 返回可直接编辑的作者草稿样板；样板历史文字是教师待填提示，不是正式课程内容 |
| `GET/POST /scenario-drafts` | 管理员 | 列表或保存服务器关卡草稿；`PUT /scenario-drafts/{scenario_id}` 使用 revision 防止陈旧页面覆盖新内容 |
| `POST /scenario-drafts/{scenario_id}/validate` | 已保存关卡草稿 | 编译严格契约并返回字段路径、错误代码和消息，不写 sealed 制品 |
| `POST /scenario-drafts/{scenario_id}/seal` | 校验通过的关卡草稿 | 自动生成不可变版本与 checksum 并登记到 runtime 暂存层；不改变学生端可见性 |
| `POST /sealed/{lesson_id}/versions/{version}/publish` | 当前工作流的 sealed 版本 | 生成整课 V2 release 并切换活动指针；`scenarios=null` 沿用、`[]` 解除、显式列表替换绑定，非空列表必须且只能有一个 `primary` |
| `POST /releases/{course_id}/bootstrap-legacy` | 无活动 release；显式列出 `lesson_id + content_version` | 对白名单旧 sealed 逐份校验后建立首个受管 release；不允许目录扫描自动发布 |
| `POST /releases/{course_id}/rollback` | 已有活动 release | 以历史 release 为目标生成新的回滚快照 |

所有 signed metadata、sealed、课程包和关卡制品在读取时都会复算 checksum，并校验规范路径、课程 ID、课时 ID、稳定制品 ID 与版本身份。单文件写入使用同目录临时文件、`flush + fsync` 和原子替换；不可变文件使用原子硬链接实现“只创建、不覆盖”。发布与回滚先复验快照内 sealed 源、内容寻址课程包、全部关卡及 `RuntimeBundleV1`，并检查跨课程 `lesson_id` 与 published `scenario_id` 唯一性，再完成 manifest 和整门课程的工作流投影，最后才原子替换 active pointer；接口返回失败时课程与游戏目录都仍读取原 release。发布前记录活动指针身份，两个并发请求从同一旧指针出发时只有一方可提交，另一方稳定冲突并要求重试。`content/releases/transactions/` 保存短生命周期恢复日志，API 重启时按活动指针完成或回滚中断事务；可重入的全局工作流文件锁串行化本机多 worker 的保存、审校、封存和发布操作。生产集群跨主机部署时仍应迁移到支持事务/CAS 的数据库或对象存储。

兼容规则：学生课程服务不扫描 `content/sealed` 猜测最新版本。历史 sealed 只有通过管理员 `bootstrap-legacy` 接口显式列出课时与版本、通过最低发布校验并生成 active pointer 后才可见；普通草稿校验和封存都不会触发迁移。内置 Python 课程继续作为没有发布覆盖时的回退。

### 4.1 V1 课程运行契约

`ARCH-001` 已在不改变现有内容接口、saga 和 sandbox 行为的前提下建立规范 V1 对象：

| 对象 | Python 模型 | 主要职责 |
| --- | --- | --- |
| 课程包 | `services.contracts.CoursePackageV1` | 表示一节可封存课时，保留现有课程字段并为人物、关键词、facts、资料和关卡建立稳定 ID |
| 关卡模板 | `services.contracts.ScenarioTemplateV1` | 描述变量、NPC、行动、条件事件、结局规则和卷宗模板；条件与效果使用带 `kind` 的可判别结构，不允许任意代码表达式 |
| 游戏会话 | `services.contracts.GameSessionV1` | 固定课程包/关卡版本与 checksum，保存完整回合、状态快照、NPC 状态、节点、摘要、叙事观察和恢复信息 |
| 史官卷宗 | `services.contracts.DossierV1` | 保存关键回合、状态轨迹、主要代价、史实解释、知识节点与关系边，并引用原始 facts 和资料 |

`TurnV1`、`NpcStateV1` 是 `GameSessionV1` 的规范嵌套对象。四个顶层对象都使用固定 `schema_version`，禁止未知字段，拒绝 `NaN`/无穷数，时间字段要求带时区；ID 保持现有 2-64 位 ASCII 规则并区分大小写。Pydantic 的 `frozen` 只阻止顶层字段重新赋值，不能深冻结嵌套列表与字典；规则运行时必须复制输入、构造并重新校验新模型，禁止原位修改 sealed 制品或会话。课程包和关卡的 `status` 只表达 `draft|sealed` 制品状态，校验、审校、发布和回滚由 `BE-001` 的独立发布记录管理。

`RuntimeBundleV1` 是跨对象语义校验入口：除版本、ID 与 checksum 绑定外，还会从场景初始值按回合重算行动可用性、状态效果、自动事件、NPC 变化、节点迁移与结局优先级，并逐项核对 `state_changes`、`fact_refs`、`ruleset_hash` 和规则叙事，再核对卷宗结局、完整状态轨迹、会话时间线和关键选择原始输入。它用于拒绝“快照结构正确但不可能由规则产生”的伪造记录。`services/game_runtime/SituationEngineV1` 已能从 sealed 课程包和关卡文件启动、推进与重放会话；每次输入先通过 JSON 投影重建并深度校验，防止嵌套列表或字典被原位改写后绕过 Pydantic 验真。

V1 回放顺序固定为：校验当前节点与行动前置条件，先应用行动效果，再基于“行动后、事件前”的状态一次性筛选事件；事件按 `priority + event_id` 稳定排序后依次应用，`once=true` 的事件只触发一次，同回合事件之间不级联选取。数值效果逐条限制在变量或 NPC 的声明范围内，`rejected|failed` 回合不得产生状态、NPC 或事件效果。首次满足结局后不得继续追加回合，节点显式结局优先，否则选择满足条件且优先级最高的结局；命中结局的会话不能伪装成 `failed|abandoned`。会话不得超过 `max_turns`，`revision` 必须恒等于 `current_turn + 1`，active 恢复态的可用行动清单也必须能从当前状态复算。`client_action_id` 重试还必须携带产生原回合时的 `expected_revision`，不能在后续修订号上把旧动作误认成新重试。生成和验真共同调用 `services/contracts/rules_v1.py`，不得另设一套隐式顺序。

sealed 契约文件使用单一二进制句柄读取：先以 `fstat` 确认为普通文件并限制为 2 MiB，再做上限加一的有界读取，最后用 `lstat` 拒绝符号链接并核对打开对象与路径对象身份。JSON 仍拒绝重复 key、非法 UTF-8、未知字段和 checksum 篡改；不能恢复为“先检查路径、再按路径另读一次”的两阶段实现。

契约文件与样板位于：

```text
services/contracts/                 Pydantic V1 模型、共享 rules_v1 reducer、跨对象校验和 legacy 适配器
services/game_runtime/              sealed 文件加载、显式场景目录、SQLite 会话/卷宗存储、行动推进、幂等、回放与卷宗派生
services/ai/                        严格行动归类、规则后受约束叙事、已审上下文筛选与稳定回退
content/schemas/v1/                 可交给其他服务使用的 JSON Schema 2020-12 文件
content/examples/v1/                大禹状态式与商鞅节点式课程/关卡技术夹具，教学内容均待教师审校
content/schemas/releases/v2/        CourseReleaseManifestV2 JSON Schema
content/examples/releases/v2/       V2 联合发布清单技术夹具，不代表实际上线内容
content/scenarios/catalog.v1.json   显式 allowlist，固定课程/关卡版本、checksum、路径与 audience
scripts/export_runtime_contracts.py 可重复生成 schema 与样板文件
tests/test_runtime_contracts.py     schema 漂移、规则回放、引用、checksum、兼容与篡改测试
tests/test_release_contracts.py     V1/V2 兼容、内容寻址、联合身份、primary、checksum 与指针测试
tests/test_joint_release_workflow.py 关卡暂存、三态绑定、V1 升级、并发、冲突、损坏回滚与课程/游戏同源测试
tests/test_game_release_catalog.py  active V2、development 隔离、完整历史身份、旧会话与篡改测试
tests/test_game_runtime.py          双模板加载、在线推进、重放、节点、NPC、终态、幂等与篡改测试
tests/test_game_api.py              目录边界、API 全链路、并发、错误映射、会话隔离与 legacy 兼容测试
tests/test_game_persistence.py      SQLite 重启恢复、跨连接 CAS、记录篡改与固定规则缺失测试
tests/test_game_dossier.py          完成态卷宗、精确回放、教师摘要、事务回滚与篡改测试
tests/test_game_narrator.py         叙事 Schema、上下文脱敏、引用白名单、单次 CAS、并发漂移与离线回放测试
tests/test_dayu_end_to_end.py        教师内容与关卡低代码生产、published pin、多回合、卷宗、画板及重启回放整链测试
tests/test_auth_api.py               scrypt、登录、Cookie/Bearer、撤销、角色管理、最后管理员和审计链篡改测试
tests/test_content_rbac_api.py       全路由权限映射、作者所有权、自审隔离、身份防伪、可信 Origin、审计阻断与并发首建测试
tests/test_game_ownership_api.py     双学生会话/卷宗/回放 IDOR、教师摘要权限、legacy 未映射数据与敏感读取审计测试
tests/test_student_resource_ownership_api.py 双学生学习进度/画板隔离、精确前缀、并发合并与 legacy 未映射数据测试
tests/test_student_asset_migration.py dry-run、原子映射、幂等回执、冲突回滚、孤立资产与 accounts 启动门禁测试
```

在仓库根目录重新生成并验证：

```powershell
& ".\.venv\Scripts\python.exe" scripts\export_runtime_contracts.py
& ".\.venv\Scripts\python.exe" -m unittest discover -s tests -v
```

正常结果为导出 V1 运行契约与 `releases/v2` 联合发布契约的 schema/样板目录，且当前后端全量 247 项测试全部通过。生成器会清理这些受管目录中不再属于清单的旧 JSON，防止废弃 schema 或样板继续被下游误读。对外 JSON Schema 要求显式提供 `status`，防止省略默认值绕过生命周期分支。V1 draft 允许正文或关卡规则暂未写完，但不能携带封存元数据；sealed 才强制正文、变量、行动、结局、版本和 checksum 完整。checksum 只能对已经通过 Pydantic 校验的模型计算，不能直接签名未经规范化的原始字典。

`course_package_from_legacy()` 会保留旧 `LessonContentPackage` 的课程元数据、`draft|sealed`、版本、时间、saga/sandbox 素材和暂未解析引用；合法的旧画布 ID 原样保留，必须替换的 ID 写入 `compatibility.legacy_id_map`。旧 sealed 内容必须先通过源 checksum 校验，源 checksum 写入 `compatibility.source_checksum`，转换后的 V1 制品再生成自己的可复算 checksum，避免把被修改的旧文件重新签成可信制品。旧格式缺少逐条事实来源绑定时不会自行推测关系。大禹治水样板中的历史正文、facts、人物 persona 和解释均标有“教师待审”，只用于开发验收。

### 4.2 联合发布 V2 契约

`ARCH-002` 新增 `services/contracts/release_v2.py`，但不原地扩写已经落盘的 `course-release/v1`。`CourseReleaseManifestV1 | CourseReleaseManifestV2` 使用 `schema_version` 判别读取；现有 `course-release-pointer/v1` 继续作为唯一活动提交点，它可以固定 V1 或 V2 manifest 的路径与 checksum，不创建第二个“关卡活动指针”。

V2 的 `RuntimeArtifactDescriptorV1` 同时固定制品种类、Schema、稳定 ID、课程/课时、版本、checksum 与规范 POSIX 路径。课程包路径只能是 `runtime/v1/course-packages/{course_id}/{lesson_id}/vNNN-{checksum}.json`，关卡路径只能是 `runtime/v1/scenarios/{course_id}/{lesson_id}/{scenario_id}/vNNN-{checksum}.json`。`CourseReleaseItemV2` 把一个 sealed 课程源、一个内容寻址课程包和零到多个内容寻址关卡绑定为单课时快照；有场景时必须声明且只能引用本条目中的 primary 场景。manifest 还拒绝跨课时重复 `scenario_id`、重复制品路径、未排序条目、身份漂移和非法 lineage。

`calculate_release_metadata_checksum()`、`sign_release_metadata()` 与 `parse_signed_course_release_manifest()` 使用与现有发布元数据一致的规范 JSON SHA-256。这里的 checksum 用于意外损坏检测，不是抵御能够重写文件并重算整条链的数字签名；生产部署需把活动指针放入具备事务/CAS 与访问控制的可信存储。`content/examples/releases/v2/` 只是 development 契约夹具，文件存在不会令其进入 published 列表。

`BE-004` 已把内容工作流、课程读取与游戏目录接入 V2：新发布一律写 `CourseReleaseManifestV2`，V1 历史仍可读并会在下一次发布或回滚时转换为 V2；课程与正式关卡只由同一个 active pointer 同时生效。静态 `content/scenarios/catalog.v1.json` 被限制为 `audience=development`，不能自我晋升为正式内容；active V2 中的场景才投影为 `audience=published`。会话恢复把课程 ID、课时 ID、课程版本与 checksum、关卡 ID、版本与 checksum 一并传给历史查找，沿当前指针的可达 parent 链加载精确 bundle，不回退到“当前”或“最新”关卡。`FE-001` 已在该严格契约前增加服务器作者草稿、校验和封存接口，并在 `/admin/content` 接入不暴露 JSON 的关卡规则表单、效果预览、刷新恢复、原始字节导出及课程联合发布选择；编辑请求期间锁定表单并以编辑代次阻止旧响应覆盖新输入，切换草稿会保护未保存修改，token 变化会清空旧请求数据并只接纳最后一次响应。真实用户鉴权与隔离沿用后续身份任务。

ADR-0013 的学生侧发布身份交接已经实现：published 课时公开最小 `scenario_refs` 与唯一 primary，正式关卡启动必须携带 release、课程包和关卡的完整 pin，并从活动指针的可达历史精确验真；development catalog 保留仅按 ID 的兼容入口。published 会话的持久 envelope 保存原始 release ID、序号与 checksum，因此后续 release 即使原样复用同一组制品，也不能用同一 `client_request_id` 把旧会话重标记为新发布。旧 V1、内置和无关卡课时返回空绑定，不从全局目录猜测；前端“练”层只使用当前课时的 primary，先持久化请求 ID，再启动或刷新恢复并核对服务端完整身份。

`FE-002` 的前端合同集中在 `apps/web/src/pages/lesson/gameSessionReference.ts`：它从当前 published 课时与 primary 场景构造 release、课程包和关卡的联合身份，读取及回写 release-scoped 浏览器会话引用，并统一复验启动响应、完成态会话与 final 卷宗。“练”层完成后直接提供“整理卷宗”入口；“创”层使用原 `client_request_id + release_pin` 幂等重启并要求得到同一 `session_id`，再读取服务器复验后的卷宗，不能信任任意本地 session ID。`LessonCreate.tsx` 展示关键选择、主要代价、历史解释和追问，把知识节点与关系按课程 checksum 和稳定 ID 合并到现有画板，并在节点与关系 data 中保存 `session_id`、`dossier_id` 与 `dossier_checksum` 来源；重复导入不增殖节点，学生原节点与位置保持不变。无会话、未完成、release 漂移、卷宗损坏或网络错误只显示失败关闭状态，不伪造成果或覆盖画板。

`BE-005` 的第一段在 `apps/api/auth_dependencies.py::require_student_context` 和 `services/game_runtime/` 建立游戏资源所有权边界。accounts 模式下，路由先通过 `GameRuntimeService.for_owner()` 取得不可改绑的学生运行时视图；启动会话只采用认证 `Principal.user_id`，请求体不能提交权威 `user_id`，会话读取、固定/自由行动、卷宗读取或补建和回放也不再暴露可选 owner 参数。底层在异步分类、叙事后的每次重读及最终 CAS 前再次核对所有者。跨用户和未知会话使用同一 `404 resource_not_found`，CAS 禁止改变既有 `user_id`。教师摘要不复用学生视图，只允许具备 `student.summary` 的教师或管理员按精确 `session_id` 读取，并把成功读取写入认证审计；当前尚无班级授权模型，因此不提供学生列表或批量摘要。`legacy-local` 仅为本地一键体验生成 `CHRONO_GAME_USER_ID` 对应的合成学生，既有本地会话在 accounts 模式不会自动归给第一个登录账号。

V0.9.4 把同一边界扩展到 `apps/api/routers/learning.py` 和 Canvas 的 GET/PUT/generate。accounts 模式的学习进度与画板分别使用 `user_id:lesson_id` 复合键；进度列表只执行转义后的所有者前缀查询，响应不回显 `user_id`，写入通过 CAS 重读并合并同时完成的层级，持续冲突返回 `409 progress_write_conflict`，损坏记录返回 `500 progress_integrity_error`。画板继续使用 `canvas/v1` 信封和 revision CAS，但每个账号拥有独立 revision 空间。`legacy-local` 明确保留旧 `default:lesson_id` 进度键与裸 `lesson_id` 画板键，accounts 永不回退读取或自动认领这些数据；V0.9.5 已提供下面的显式映射路径。

V0.9.5 的 `services/persistence/student_assets.py`、`scripts/create_student_account.py` 与 `scripts/migrate_student_assets.py` 收口显式映射。迁移命令要求现有 SQLite 文件、通过密码登录得到的真实管理员短时会话、已启用且具备 `student` 角色的目标账号，以及精确旧 `CHRONO_GAME_USER_ID`；纯管理员不能作为临时目标。默认只生成包含资产数量与源指纹的计划，只有 `--apply` 才在单一事务中移动 `default:*` 进度和裸画板键、重签 V1/V2 会话信封及 final 卷宗 checksum，并同时写入带 actor/session/request/target/counts/fingerprint/checksum 的不可覆盖回执和认证审计。目标已有同课时进度或画板、未知或非学生所有者、孤立卷宗、损坏 JSON/checksum、并发源变化、审计失败或回执重现都会整笔回滚；重复执行已完成迁移返回同一 `already_applied` 回执。会话 ID、卷宗 ID、课程/关卡 release 身份、画板原始 JSON 和领域时间保持不变。accounts lifespan 在内容或游戏服务对外可用前扫描所有学生资产，发现 legacy、未知账号或已知但非学生账号所有者便以 `unmapped_student_assets` 拒绝启动，不再静默隐藏。

## 5. 系统心智模型

Chronovita 把历史课组织为两个轴：

| 主轴 | 表达 | 入口 |
| --- | --- | --- |
| 平台模块轴 | 首页 / 课程中心 / 我的学习 / 实践课堂 / 个人中心 | `apps/web/src/App.tsx` 与 `apps/api/routers/` |
| 课内四层轴 | 看 / 练 / 问 / 创 | `apps/web/src/pages/lesson/` 与 `apps/api/routers/practice.py` |

当前是开发期单体架构：

```text
React + Vite + TypeScript 前端
        |
FastAPI API 层
        |
services 业务层
        |
SQLite KV 持久化 + LLM 适配层
```

## 6. 仓库结构

```text
.
├── apps/
│   ├── web/                  React 前端平台
│   └── api/                  FastAPI 后端
├── services/
│   ├── courses/              课程数据与教材级正文
│   ├── saga/                 LLM 互动剧本引擎
│   ├── sandbox/              参数化决策沙盘
│   ├── llm/                  LLM 适配层
│   ├── contracts/            课程、关卡、会话与卷宗 V1 运行契约
│   ├── game_runtime/         文件驱动历史局势引擎核心
│   ├── content/              内容 schema、审校状态机、封存、发布与回滚
│   ├── operations/           生产配置门禁与运行依赖探针
│   └── persistence/          SQLite KV 持久化
├── content/                  草稿、封存、V1 制品、工作流与课程发布清单
├── docs/                     5 份核心文档 + ADR + archive
├── scripts/                  saga 批量/压力测试脚本
├── assets/design/            Pencil 设计稿
├── UI/                       美术视觉稿
└── data/                     运行期 SQLite 数据库（git 忽略）
```

## 7. 前端入口

| 我想改 | 文件 |
| --- | --- |
| 顶栏、路由、Logo、搜索 | `apps/web/src/App.tsx` |
| 首页 | `apps/web/src/pages/HomePage.tsx` |
| P0 路线 | `apps/web/src/pages/p0Route.ts` |
| 课程中心地图 | `apps/web/src/pages/CoursesPage.tsx` + `pages/courses/` |
| 课程详情探索盘面 | `apps/web/src/pages/CourseDetailPage.tsx` |
| 我的学习 | `apps/web/src/pages/LearningPage.tsx` |
| 实践课堂入口 | `apps/web/src/pages/PracticePage.tsx` |
| 个人中心 | `apps/web/src/pages/ProfilePage.tsx` |
| 管理员内容编辑器 | `apps/web/src/pages/AdminContentPage.tsx`；记忆上次选择的课程、人物、关键词或关卡编辑模式 |
| 关卡规则低代码编辑 | `apps/web/src/pages/admin/ScenarioRuleEditor.tsx` + `scenarioRuleModel.ts`；表单状态先本地暂存，再经管理员 API 保存、校验、封存和精确重读 |
| 课程关卡联合发布 | `apps/web/src/pages/AdminContentPage.tsx` + `apps/web/src/utils/api.ts`；将保留映射为省略 `scenarios`、清空映射为 `[]`、更新映射为带唯一 `primary` 的精确版本列表 |
| 课内四层容器 | `apps/web/src/pages/lesson/LessonPage.tsx` |
| 看 | `LessonWatch.tsx` |
| 练 | `LessonPractice.tsx`；V2 正式关卡由 `lesson/GamePractice.tsx` 负责完整 release pin 启动、刷新恢复、revision 推进和显式重开，并通过 `lesson/gameSessionReference.ts` 共享发布身份与本地会话引用；内置/未发布课程继续保留 legacy Saga/Sandbox |
| 问 | `LessonAsk.tsx` |
| 创 | `LessonCreate.tsx`；在共享 release/session 身份复验后读取 final 卷宗，展示选择、代价、解释与追问，并把稳定知识节点/关系幂等合并到现有 React Flow 画板；自动保存使用 revision CAS，冲突时锁定编辑并要求载入服务器最新版本 |
| 随行助教 | `apps/web/src/components/CompanionPanel.tsx` |
| API 客户端 | `apps/web/src/utils/api.ts` |
| 主题与样式 | `theme.ts` + `styles/global.css` |

## 8. 后端入口

统一前缀：`/api/v1`。

| 路由模块 | 前缀 | 职责 |
| --- | --- | --- |
| `common.py` | `/api/v1` | 版本、模块导航 |
| `auth.py` | `/api/v1/auth` | Cookie 登录、Bearer 签发、当前主体、原子刷新、幂等退出、本人/管理员全会话吊销、用户/角色/密码管理与认证审计读取 |
| `admin_content.py` | `/api/v1/admin/content` | 管理员内容草稿、校验、审校、封存、发布、回滚与资料档案 |
| `home.py` | `/api/v1/home` | 首页占位接口 |
| `courses.py` | `/api/v1/courses` | 时代、课程、课时 |
| `learning.py` | `/api/v1/learning` | 学习进度 |
| `practice.py` | `/api/v1/practice` | saga、sandbox、ask、canvas、LLM info |
| `game.py` | `/api/v1/practice/game` | V1 场景清单、持久会话启动/恢复与权威回合推进 |
| `profile.py` | `/api/v1/profile` | 个人中心占位/本地资料辅助 |

启动入口：

| 文件 | 职责 |
| --- | --- |
| `apps/api/main.py` | FastAPI 应用、路由挂载、CORS、请求遥测中间件、lifespan 初始化与生产异常收束 |
| `apps/api/settings.py` | `CHRONO_` 环境变量配置、显式 `local|production` 运行档位；数据库 URL、admin token、bootstrap 密码和模型 key 使用 `SecretStr`；另含兼容 SQLite 路径、身份模式/会话/Cookie、内容与场景目录、legacy 本地游戏用户和 LLM provider |
| `apps/api/auth_dependencies.py` | Bearer/Cookie/legacy token 解析、统一 `Principal`、显式动作权限与学生所有者依赖；冲突凭据失败关闭 |
| `apps/api/.env.example` | 本地接入 LLM 的配置示例 |

## 9. 服务层职责

| 服务 | 当前职责 | 后续演进 |
| --- | --- | --- |
| `services/auth/` | 账号、scrypt 密码、通道绑定且只存摘要的不透明会话、闲置/固定/绝对寿命、CAS 轮换、用户全会话吊销、显式角色动作和带前序摘要的追加式认证审计；内容管理写入与教师敏感摘要读取登记认证授权事件；Store 不建表，API/脚本必须先由统一 schema 管理器复验表结构和审计链 | 后续增加终端会话清单和跨实例共享认证限流 |
| `services/courses/` | 内置课程与 active V1/V2 release 的学生读取投影；V2 课时附带 release 身份、最小关卡 refs 与唯一 primary，V1/内置内容返回空绑定，并对损坏发布失败关闭 | 后续接入班级与课程授权 |
| `services/content/` | 严格内容 schema、原子草稿/封存、sealed 关卡登记、内容寻址课程/关卡物化、V1/V2 manifest、恢复日志、单活动指针与整套回滚 | 本机多 worker 使用可重入全局工作流文件锁；跨主机集群迁移到事务数据库或支持 CAS 的对象存储 |
| `services/contracts/` | V1 课程运行契约、跨对象引用校验、checksum 和旧内容包适配 | 供发布、局势引擎、会话持久化与 AI 编排共同使用 |
| `services/game_runtime/` | 合并 development 静态 allowlist 与 active V2 的 published 关卡，按完整 release pin 从可达历史精确启动；以带 checksum 的 SQLAlchemy JSON 镜像保存完整会话/卷宗及 published 启动身份；accounts API 按认证学生复核全部精确访问和 CAS，Store 不建表，并按七段制品身份恢复与重放 | 后续接入班级授权与学情聚合 |
| `services/ai/` | `ActionClassifierV1` 把自由文本限制到当前行动；`HistoricalNarratorV1` 只读取固定规则投影、已审 facts/source ID 和 persona/boundaries，输出有限文本与引用，失败时原样回退规则叙事 | 后续增加调用租约、成本治理与更细的人物发言模板；不在本层实现规则效果或 RAG |
| `services/saga/` | 互动小说式 LLM 推演，48 节模板覆盖 | 接入回放、状态摘要、关卡模板 |
| `services/sandbox/` | legacy 参数化决策沙盘，当前商鞅变法硬编码示例；其 step 接收客户端 state，不是 V1 权威状态源 | 保留兼容；新关卡统一进入 `services/game_runtime/` |
| `services/llm/` | mock/DeepSeek 兼容流式适配；DeepSeek 非流式严格 JSON、类型化故障、响应大小/超时边界和规范输出 checksum | 行动分类与规则后叙事均复用此适配器；多模型路由、成本治理和缓存仍待后续任务 |
| `services/operations/` | `local|production` 配置预检、数据库/schema 就绪基线、轻量连接探针、请求 ID ContextVar 与响应头、数据最小化 JSON 访问事件、生产异常脱敏、精确 Host 边界和单进程登录令牌桶 | 后续接入真实 Postgres 服务探针与跨实例共享限流 |
| `services/persistence/` | 共享 SQLAlchemy Engine、SQLite KV 与业务 JSON 镜像基础；`database.py` 解析并脱敏 SQLite/Postgres 目标、固定同步驱动及 Engine 参数；`schema.py` 是唯一建表、迁移、认领与审计头初始化入口；`backup.py` 管理 SQLite 一致快照、manifest、完整性验证、安全恢复与回滚；`student_assets.py` 负责独立的学生资产扫描、dry-run/原子映射和 checksum 回执 | 真实 Postgres 服务、TLS、池容量、原生备份及离线业务命令扩展由 OPS/QA 后续处理 |

## 10. 核心数据流

### 10.1 课程读取

```text
前端课程页
  -> GET /api/v1/courses
  -> courses.list_courses()
  -> 内置课程 + 每门课程 active V1/V2 release 指定的课程包

课时页
  -> GET /api/v1/courses/{course_id}/lessons/{lesson_id}
  -> courses.get_lesson(lesson_id)
  -> load_published_snapshots() 严格校验 pointer、manifest、旧 sealed、内容寻址课程包与关卡绑定
  -> 若存在已发布 lesson_id，返回正文、关键词、人物、地图点、资料与 release 身份
  -> 否则返回内置课程正文、关键词、人物、seed canvas、sandbox_id
  -> 任一已发布制品损坏时返回 503，不扫描目录猜测“最新版本”
```

### 10.2 saga 推演

```text
LessonPractice
  -> POST /practice/saga/start {lesson_id}
  -> require_student_context() 取得认证 student
  -> saga.start(lesson_id, owner_user_id, TTL, 用户/全局容量)
  -> 开场、候选行动、persona、keywords

学生行动
  -> POST /practice/saga/{saga_id}/act {action}
  -> 所有者核对 + 单 Saga 动作租约 + 用户模型额度
  -> saga.act_stream(max_response_chars)
  -> llm.stream_chat()
  -> text/plain 流式正文 + [META] JSON
```

saga 模板包含 `facts`、`era`、`setting`、`persona`、`opening`、`initial_choices`、`keywords`。accounts 模式要求真实 `student`，跨用户读取与行动统一返回相同 404；`legacy-local` 继续使用合成本地学生，供一键编辑体验使用。状态只保存在当前 API 进程，使用单调时钟 TTL、每用户保留量和全局保留量限制；终局 Saga 仍计入用户保留量且读取不续期。模型 META 先经严格 Pydantic 校验和重复键拒绝，再在锁内原子应用；第 7 回合由服务端强制结束，不依赖提示词。模型异常、输出超限、总时限、任务取消、响应头前断连和首块后断连都会回滚未提交行动，并通过响应级一次性清理归还动作与并发租约。

### 10.3 sandbox 决策沙盘与 V1 局势核心

legacy 接口保持兼容：

```text
GET /practice/sandbox/{sid}
  -> scenario、start node、init_state

POST /practice/sandbox/{sid}/step
  -> sandbox.step()
  -> 下一节点、feedback、新 state
```

legacy `sandbox.step()` 信任客户端传入 state，只供旧页面兼容，禁止用于 V1 会话。V1 核心调用链为：

```text
SituationEngineV1.from_files(course_path, scenario_path)
  -> UTF-8 JSON、重复 key、sealed 状态、checksum 与 RuntimeBundle 跨引用校验
  -> start_session(...)
  -> 初始变量、NPC、节点、可用行动与精确制品身份

apply_action(session, RuntimeCommandV1)
  -> 复验完整历史和 expected_revision
  -> rules_v1.evaluate_rule_action()
  -> 行动效果 -> 事件筛选/执行 -> 节点迁移 -> 结局裁定
  -> 生成 TurnV1 与新 GameSessionV1
  -> RuntimeBundleV1 再次确定性回放验真
```

纯规则核心可按 `action_id`、label 或 alias 确定性解析行动，本身不会用模糊字符串或 LLM 猜测行动。API 协调层中，固定按钮只提交 `action_id`，服务端从固定关卡制品取得教师标题并写入回合；自由文本则先由 `ActionClassifierV1` 映射到当前可用行动，再以 `action_source=free_input` 进入同一规则核心。相同 `client_action_id` 和相同输入返回原结果，不同输入复用该 ID 会冲突；`expected_revision` 防止并发覆盖。引擎不在原模型上修改嵌套字典或列表。达到 `max_turns` 却没有模板结局、或未到上限但没有可用行动时，关闭为可解释的 `failed` 状态。

V1 API 调用链：

```text
GET  /api/v1/practice/game/scenarios
  -> 合并 catalog.v1.json 的 active development 条目与 active V2 release 的 published 条目
  -> 返回固定版本/checksum、课程身份、audience 与关卡摘要

POST /api/v1/practice/game/sessions {scenario_id, client_request_id, release_pin?}
  -> published 场景必须提交 release ID/序号/checksum、课程/课时、课程版本/checksum 与关卡版本/checksum；development 场景可省略 pin
  -> 服务端只沿当前活动指针的可达 V2 parent 历史查找完全匹配的发布，不可达、错绑或损坏制品均不创建会话
  -> client_request_id 必填；相同用户、release 与七段制品身份重试返回同一会话，任一身份变化返回 duplicate_start_conflict
  -> published 摘要回显服务端验真的 release 身份，调用方同时核对 session 中的七段制品身份

GET  /api/v1/practice/game/sessions/{session_id}
  -> 从 SQLite 读取带 checksum 的完整会话
  -> 按课程/课时/课程版本与 checksum、关卡 ID/版本与 checksum 从静态制品或可达 release 历史精确加载并重建 RuntimeBundleV1 验真

POST /api/v1/practice/game/sessions/{session_id}/turns
  -> 固定行动只接收 client_action_id、action_id、expected_revision
  -> raw_input、action_source、classification_confidence、state/current_turn/ending 等客户端权威声明因 extra=forbid 被拒绝
  -> 服务端从固定关卡制品读取行动标题，来源固定为 fixed，置信度固定为 1.0
  -> 先生成纯规则候选，再在状态锁和数据库事务外请求受约束叙事；返回后复核原 revision，并把 Turn、叙事证据和完成态卷宗一次 CAS 提交

POST /api/v1/practice/game/sessions/{session_id}/free-input
  -> 只接收 client_action_id、raw_input、expected_revision，拒绝客户端候选 action_id 与分类元数据
  -> 精确 ID/标题/alias 免模型；其余输入只能由结构化分类器映射到当次可用行动
  -> advanced 才产生 Turn 并使 revision + 1；clarification_required/rejected/provider_unavailable 返回安全提示和可用行动但不写会话
  -> 分类在状态锁与数据库事务外执行，返回后重新读取 revision；期间发生其他回合则返回 revision_conflict，不在新状态上自动重试分类
  -> matched 分支同样先结算纯规则候选，再在锁外生成叙事并复核 revision；叙事失败不撤销合法行动，而是以稳定原因码和逐字规则文本完成同一次 CAS

GET  /api/v1/practice/game/sessions/{session_id}/replay
  -> 从固定课程/关卡制品重新推导规则状态、basis、history、summary 与卷宗引用
  -> 复用通过证据校验的持久叙事，不调用分类器、叙事器或外部供应商

GET  /api/v1/practice/game/sessions/{session_id}/dossier
  -> 只读取并复验已物化卷宗；不会因 GET 产生写入

POST /api/v1/practice/game/sessions/{session_id}/dossier
  -> 仅为旧完成态会话显式幂等补建卷宗

GET  /api/v1/practice/game/sessions/{session_id}/summary
  -> 返回教师可读的会话、选择、轨迹、facts、资料与卷宗身份摘要
```

场景路径相对 `CHRONO_CONTENT_ROOT`，必须是规范 POSIX JSON 路径；仓库不扫描目录猜测关卡。静态 catalog 只允许 `audience=development`，正式关卡只来自 active V2 release；同一 `scenario_id` 正式发布后，active published 投影覆盖 development 列表项，旧开发会话仍按完整身份从静态制品恢复。catalog、pointer、manifest 和制品都使用严格读取与完整身份校验，拒绝逃逸、符号链接、重复 key、路径身份或 checksum 漂移。同一课程包可供多个关卡复用，但路径对应的课程 ID、课时 ID、版本与 checksum 必须完全一致。

API 的 `session_storage=sqlite-json` 表示完整 `GameSessionV1`（含 Turns、状态快照、NPC、结局）是唯一规则状态事实源，`game_sessions` 表只保存 `session_id / data / updated_at`。`data` 使用规范 JSON、4 MiB 上限和会话级 SHA-256 envelope；新写入统一使用 `persisted-game-session/v2`，published 会话还固定 release ID、序号与 checksum。旧 `persisted-game-session/v1` 必须先对原始 JSON 中尚未补默认字段的 `session/release_identity` 按旧算法核对 checksum，再映射为 `ai_evidence_version=0`；下一次成功 CAS 或卷宗挂接才原子写成 V2，并原样保留 release identity。V1 不允许伪装携带新证据字段。release 元数据只约束启动来源和幂等，不参与规则结算。读取先拒绝损坏 JSON、重复 key、身份/checksum 不一致，再按会话固定的关卡版本与 checksum 加载原制品，并用 `RuntimeBundleV1` 重推规则和证据 basis。写回必须使 revision 恰好增加一，并用“主键 + 原始整段 data”执行原子 compare-and-swap；同进程 `RLock` 降低冲突，其他进程或连接仍由数据库 CAS 裁决。分类与叙事调用都位于状态锁和数据库事务外；叙事前只形成不可见的规则候选，叙事返回后再次读取原 revision，未漂移时才把规则状态、展示文本、类型化证据及可选完成态卷宗一次 CAS 提交。自由输入同一已提交请求跨服务竞争只保留一个赢家，即使两次模型分类或叙事不同也恢复数据库中的既有回合；其他陈旧动作返回 revision 冲突，旧叙事不会套到新状态。当前不持久化澄清、拒绝或供应商失败的 attempt receipt，也不提供模型请求租约，因此零写入结果或 CAS 决胜前的竞争请求可能重复调用供应商；该边界影响调用成本，不会重复推进状态。应用 lifespan 关闭后 SQLite 文件保留，重启可继续游玩；开发期仍只有单一配置用户，尚无生产级用户隔离。

`game_dossiers` 表以相同 envelope 方式保存完整 `DossierV1`，单记录上限为 2 MiB。最终回合与卷宗在同一数据库事务中完成，会话不会先进入完成态再留下悬空 `dossier_id`；旧完成态会话只能通过显式 POST 在同一事务中补建并挂接。卷宗 ID、标题、选择、状态轨迹、代价、解释、facts、资料、反思问题和允许的知识节点均由固定场景、课程字段及真实回合确定性派生；没有封存关系契约时不编造知识边，也不代写学生或教师反思。读取时除 checksum 和 `RuntimeBundleV1` 外还会重新派生并逐字段比对，因此“修改文本后重算 checksum”仍会失败关闭。

这里的 `final` 只表示一次会话的不可变技术制品已经生成，不等同于内容通过历史审校或进入正式发布。只有 catalog 中明确标记为 `audience=published` 且走完课程/关卡联合发布流程的制品，才可作为正式教学版本；当前两份样板仍只用于开发验收。

### 10.4 ask 双模态问答

```text
POST /practice/ask
  -> require_student_context()
  -> 用户令牌桶 + 并发租约 + 总时限/输出上限
  -> practice._system_prompt(persona)
  -> llm.stream_chat(model=deepseek_model_pro)
  -> text/plain 流式返回
```

| persona | 角色 | 约束 |
| --- | --- | --- |
| `expert` | 历史专家/教师 | 教材共识、争议标注、不编造、250 字内 |
| `peer` | 历史人物同伴 | 第一人称、时代边界、不预言后世、200 字内 |

### 10.5 canvas 画板

```text
GET /practice/canvas/{lesson_id}
PUT /practice/canvas/{lesson_id}
POST /practice/canvas/generate
```

画板保存在 SQLite KV。accounts 模式使用 namespace=`canvas`、key=`user_id:lesson_id`；`legacy-local` 为保持一键本地数据兼容，继续使用裸 `lesson_id`。两种模式不会互相回退读取。`POST /canvas/generate` 还要求学生身份，限制标题、摘要、关键词、种子和模型输出大小，并与 Saga/Ask 共用当前进程的用户令牌桶、并发租约和总时限。

GET 返回 `schema_version="canvas/v1"`、`found`、`revision`、`nodes` 和 `edges`。未创建画板是 `found=false, revision=0`，与教师或学生明确保存的空画板区分；旧 `{nodes, edges}` 记录按 revision 1 读取，并在下一次成功保存时迁移为版本化信封。PUT 必须携带 `expected_revision`，服务端通过 `kv_compare_and_set()` 在数据库写锁内按完整结构化 JSON 值和独立 presence 位执行原子比较交换，成功后 revision 加一；旧记录的空白与对象键顺序不制造虚假冲突，存储 `null` 也不会与键不存在混淆。同一快照的并发写入只有一个成功，陈旧写返回 `409 / canvas_revision_conflict` 且不覆盖新图。损坏或未知结构的存储记录返回 `500 / canvas_integrity_error` 并失败关闭，不以种子节点覆盖原记录。

`LessonCreate.tsx` 串行化自动保存请求并记录当前 lesson、加载代次、编辑代次与服务器 revision；非用户触发的 React Flow 尺寸或选择变化不标记 dirty，拖动、增删、连边、AI 扩充和卷宗导入才触发 debounce 保存。遇到 `409` 后画板进入只读冲突态，只允许重新载入最新版本，不提供盲目覆盖。React Flow 仅在当前“创”层可见且父容器已测得非零尺寸后挂载，避免隐藏页签或响应式切换产生零尺寸实例。

### 10.6 学习进度

```text
POST /learning/progress/touch
namespace="lesson_progress"
accounts key="{user_id}:{lesson_id}"
legacy-local key="default:{lesson_id}"
```

`/learning`、列表、latest、精确读取和 touch 均经过 `require_student_context`。accounts 模式只从认证主体取得所有者，按转义后的 `user_id:` 前缀列举，不能通过 lesson ID 传入别人的键；旧 `default:*` 数据不会自动归给真实账号。touch 对完整记录执行 CAS，竞争写入失败后重读并保留其他请求已经完成的层级，最多重试 8 次。未知 layer 返回 `422 invalid_progress_layer`，存储身份、课时或结构不一致返回 `500 progress_integrity_error`，持续竞争返回 `409 progress_write_conflict`。

## 11. 持久化设计

当前使用 `services/persistence/db.py` 的通用 KV 表：

```text
namespace
key
data TEXT-json
updated_at
```

当前命名空间：

| namespace | 用途 |
| --- | --- |
| `lesson_progress` | 学习进度 |
| `canvas` | 知识画板 |

`kv_list_prefix(namespace, key_prefix)` 使用 SQLAlchemy 的转义前缀表达式返回键和值，专用于所有者范围查询；调用方仍必须校验键中课时 ID 与 JSON 内的 `user_id/lesson_id` 一致。复合键是当前 SQLite KV 的业务隔离合同；V0.9.7 的 schema 账本校验表结构，不把这些 JSON 镜像拆成新的关系表。

`student_asset_migrations` KV 命名空间保存显式旧学生资产映射回执，不保存密码、token、课文、学生输入或完整资产正文。回执固定来源、操作者、目标账号、各类数量、源指纹、UTC 时间和自身 checksum；普通 API 不提供删除或改写入口。该回执证明本次映射输入与结果，不替代数据库备份或 DATA-001 的 schema 迁移日志。

身份服务另使用 `auth_users`、`auth_sessions`、`auth_audit_head` 与 `auth_audit_events` 四张 SQLAlchemy Core 表。账号安全字段变化或本人/管理员执行全会话吊销时递增 `auth_version`，并在同一数据库事务内撤销该用户全部会话；会话表只保存完整令牌的 SHA-256 摘要。令牌明文由 `cvs_cookie_` / `cvs_bearer_` 命名空间、签发时固定的 UTC 绝对截止微秒和随机秘密组成，三者共同进入摘要：改写通道或截止时间都会使摘要失配，服务重启或调高/调低当前绝对寿命配置也不会改变既有会话截止时间。会话读取同时检查令牌固定截止、数据库 `expires_at` 与 `last_seen_at + idle_timeout`；刷新在 SQLite `BEGIN IMMEDIATE` 或 PostgreSQL 中统一按“用户行 -> 会话行 -> 审计头”加锁并替换旧摘要，延长结果不得超过令牌固定截止，两个并发刷新只能一个成功。刷新与撤销、禁用或改密竞争时安全变更最终使新旧令牌全部无效，不形成反向锁序。刷新、吊销与对应审计位于同一事务，审计失败时令牌和 `auth_version` 均回滚。首次管理员、普通账号创建和账号修改统一进入身份变更事务：SQLite 以 `BEGIN IMMEDIATE` 串行化，PostgreSQL 以事务级 advisory lock 串行化；空库判断与 bootstrap 写入、最后管理员计数与角色/启用状态更新都在锁内完成。审计事件按序保存前一事件摘要和当前摘要；每次追加先对唯一审计头执行 `SELECT ... FOR UPDATE`，再在同一业务事务内更新头并插入事件，使不同进程和 Engine 共享数据库顺序。`GET /api/v1/auth/audit` 在 PostgreSQL 上以 `REPEATABLE READ` 复验整条链。

`services/persistence/schema.py` 是应用启动的 schema 权威入口。`chronovita_schema_migrations` 以连续整数版本保存稳定 migration ID、契约 checksum、UTC 应用时间和首次应用版本；当前版本 1 管理 `kv`，版本 2 管理 `game_sessions/game_dossiers`，版本 3 管理身份与审计四表及审计链不变量。`persistence.init_engine()` 在返回共享 Engine 前调用 `ensure_current_schema()`：SQLite 使用 `BEGIN IMMEDIATE` 串行化真正需要升级的迁移；PostgreSQL 先取得会话级 advisory lock 并提交锁获取事务，再开启新的 `REPEATABLE READ` 迁移事务，提交或回滚后显式释放锁。这样等待者的迁移快照一定建立在前一迁移提交之后。只读结构/审计复验在 PostgreSQL 使用 `REPEATABLE READ`，在 SQLite 则显式发送 deferred `BEGIN`，避免 Python `sqlite3` legacy transaction mode 对纯 SELECT 不开启数据库事务；因此表反射、账本、审计头和事件来自同一个 WAL 读快照，同时不阻塞并发写者。所有 DDL、初始化与账本追加位于同一事务，失败会回滚并关闭 Engine；已是最新版时只读复验后直接返回，不取得写锁。无账本旧库的完整表组必须形成从版本 1 开始的连续前缀，并且列、类型、空值、主键、唯一约束和索引逐项一致才可认领；整组前序表缺失、部分表、未知应用表或任何结构/账本/审计漂移都会抛出稳定 schema 异常，不使用 `IF NOT EXISTS` 掩盖损坏。审计事件按查询游标逐条复验，不在内存中复制整条事件列表。

`CHRONO_DATABASE_MIGRATION_MODE` 默认 `apply-safe`，适用于本地离线开发和自动化测试；设为 `validate` 时只读复验且数据库不是最新版本便拒绝启动。非空 `CHRONO_DATABASE_URL` 优先于 `CHRONO_SQLITE_PATH`：`postgresql://` 会规范为 `postgresql+psycopg://`，其他同步驱动以外的 driver 或其他 dialect 失败关闭；URL 在 Settings 中按秘密值保存，构造错误不回显原始 URL。SQLite URL 的相对路径仍以仓库根为准，显式内存库使用单连接 `StaticPool` 保持跨线程一致。V0.9.10 起 `AuthStore` 与 `GameRuntimeStore` 构造器只保存 Engine，不建表、不补审计头、不校验或迁移结构；调用方必须先经过 `ensure_current_schema()`，专门模拟旧库/损坏库的测试才可显式手工建表。V0.9.8 的 `status/migrate/backup/verify-backup/restore` 仍只支持 SQLite；V0.9.9 只承诺 Postgres URL、Psycopg 驱动加载、DDL 编译和迁移方言边界，真实服务、TLS、连接池容量、命令凭据和原生备份尚未验收。

## 12. LLM 适配层

兼容流式入口：

```python
stream_chat(messages, provider=None, model=None)
```

严格结构化入口：

```python
result = await complete_json(messages, ResponseModel, model=None)
# 或注入 transport/fake 进行离线测试
result = await StructuredLLMAdapter(...).complete(messages, ResponseModel)
```

| provider | 说明 |
| --- | --- |
| `mock` | 离线兜底，保障课堂主流程不中断 |
| `deepseek` | OpenAI 兼容协议，支持 `deepseek-v4-flash` 与 `deepseek-v4-pro` |

模型分工：

- saga：速度优先，默认 `deepseek-v4-flash`。
- ask：准确度优先，显式 `deepseek-v4-pro`。
- 异常或无 API Key：回落 mock。

规则：

- 业务层不得直接拼 LLM HTTP。
- LLM 不决定史实真值；当前史实边界来自课程 `facts`、`source_refs` 和教师审校，RAG 尚未实现。
- mock 回退是课堂可靠性设计，不是临时调试残留。
- `stream_chat()` 保持 legacy saga/ask 兼容；HTTP 错误、网络异常和空流只返回稳定离线提示与 mock 文本，不向学生暴露供应商 body 或异常原文。
- `complete_json()` 不伪造通用 mock 结构。provider 未配置、超时、限流、HTTP 失败、超长、截断、重复 key、非对象 JSON 或 Pydantic 校验失败均抛出只含稳定 `LLMFailureCode` 的 `StructuredLLMError`，由分类/叙事业务层决定“不推进回合”或“使用规则文本”。
- `StructuredLLMAdapter` 自动附加响应模型 JSON Schema，启用 `response_format=json_object`，禁用 V4 thinking，分块读取并在配置上限立即中止；规范化输出只保存 SHA-256 checksum，不保留原始供应商响应。

结构化配置位于 `apps/api/settings.py` 与 `apps/api/.env.example`：

| 环境变量 | 默认值 | 边界 |
| --- | --- | --- |
| `CHRONO_LLM_STRUCTURED_TIMEOUT_SECONDS` | `12` | `1-60` 秒；超时转换为 `provider_timeout` |
| `CHRONO_LLM_STRUCTURED_MAX_TOKENS` | `512` | `32-4096`；调用方还可在更小范围覆盖 |
| `CHRONO_LLM_STRUCTURED_MAX_RESPONSE_BYTES` | `32768` | `1024-262144` 字节；按响应流累计限制 |
| `CHRONO_DEEPSEEK_BASE_URL` | `https://api.deepseek.com` | production 必须为 HTTPS、无 URL 凭据/query/fragment，且主机位于显式白名单 |
| `CHRONO_DEEPSEEK_ALLOWED_HOSTS` | `["api.deepseek.com"]` | production DeepSeek 密钥可发送到的小写精确非本地主机白名单 |
| `CHRONO_PRACTICE_LLM_RATE_LIMIT_REQUESTS` | `20` | 每名学生在补充窗口内的 Saga/Ask/Canvas 模型调用容量 |
| `CHRONO_PRACTICE_LLM_RATE_LIMIT_WINDOW_SECONDS` | `60` | `1-3600` 秒；令牌线性补充窗口 |
| `CHRONO_PRACTICE_LLM_MAX_CONCURRENT_PER_USER` | `2` | 每名学生的并发模型调用上限 |
| `CHRONO_PRACTICE_LLM_TIMEOUT_SECONDS` | `90` | `5-300` 秒；单次遗留流式调用总时限 |
| `CHRONO_PRACTICE_LLM_MAX_RESPONSE_CHARS` | `8192` | `1024-262144` 字符；Saga/Ask/Canvas 输出硬上限 |

结构化适配器的离线验证位于 `tests/test_llm_adapter.py`。测试使用 `httpx.MockTransport`，不访问真实模型，也不读取或保存真实 API key。

### 12.1 服务端行动归类器

`services/ai/ActionClassifierV1` 是自由文本进入规则引擎前的纯归类服务：

```python
classification = await ActionClassifierV1().classify(engine, session, raw_input)
```

- 输入先按 `PlayerInput` 做严格长度与空白校验；会话不是 active、没有可用行动或输入非法时直接拒绝。
- 稳定 `action_id`、教师标题和 alias 先由 `SituationEngineV1.resolve_action_id()` 精确解析，命中当前可用行动时不调用模型；精确命中但当前不可用时失败关闭。
- 只有精确解析失败且存在与当前关卡/行动相关的已审 facts 时才调用结构化分类器。模型上下文不包含 `teacher_note`、资料路径、密钥、规则效果或完整会话状态。
- `ActionClassificationV1` 只能表达当前行动匹配、需要澄清、拒绝或供应商不可用；匹配的 `action_id` 必须属于服务端当次计算的 `available_action_ids`，LLM 来源还必须带 provider、model 与规范输出 checksum。
- 低置信度、未知行动、提示注入、越时代、事实冲突、输出异常与供应商异常都不会修改传入会话。归类器本身不调用 `apply_action()`，规则引擎仍是状态、事件、NPC 和结局的唯一权威。

`GameRuntimeService.apply_free_input()` 已把纯归类服务接入独立 API：模型调用不占状态锁，返回后重新核对相同 revision，并只把 `matched` 结果交给规则引擎；澄清、拒绝和分类供应商故障均以 `FreeInputResultV1` 零写入返回。固定行动 API 使用异步 `submit_fixed_action()` 生成服务端标题、来源与置信度；同步 `apply_fixed_action()` 只保留给纯规则兼容与内部测试，客户端不能提交这些权威字段。

`HistoricalNarratorV1` 使用 `NarratorModelOutputV1`，模型只能返回 1—1200 字展示文本和不超过 32 个实际使用的 fact/source ID，未知字段、重复/越界引用、超长文本、空/截断响应或供应商故障均回退为逐字 `render_rule_narrative()`。上下文只包含固定课程/关卡身份、已结算行动、状态差、NPC 变化、触发事件、结局投影、已审 facts/source ID 以及关卡 persona/boundaries；不发送学生原文、教师备注、资料路径、密钥、规则 effects 或既有模型历史。带 `pending` 资料或“教师待审”标记的 facts 不会触发叙事供应商。

新服务会话使用 `ai_evidence_version=1`。每个 applied Turn 同时保存严格 `ActionClassificationEvidenceV1` 与 `NarrativeEvidenceV1`：前者固定分类 policy、回合前可用行动、已审 fact 白名单、provider/model、模型输出 checksum 与规则快照 basis；后者固定同一次规则结算 basis、允许及实际使用的 fact/source ID、文本 checksum、provider/model 或稳定 fallback 原因。`SituationEngineV1.with_narrative()` 会重新计算规则结果与叙事 basis，只替换 Turn/history 中的展示文本，再用 `RuntimeBundleV1` 复验状态、NPC、事件、结局、两层 basis、`history`、`summary` 和卷宗 consequence。旧版本 0 会话只允许可重算的 `rules` 叙事，不补造历史证据；`replay_session()` 直接返回验真的持久叙事，不调用分类器、叙事器或供应商。

`PublishedGamePractice` 已接入 `gameFreeInput()`：学生可在固定行动之外输入不超过 400 字的自拟行动，Enter 提交、Shift+Enter 换行；请求期间固定、自由和“重新开始”控件共同锁定，避免旧回合结果覆盖新 session。`advanced` 必须返回完整、同 release 身份的会话才会替换当前历史并清空输入；澄清、拒绝和供应商不可用只显示可关闭提示，保留原输入且不伪造回合。网络或并发错误会重新读取服务器权威会话，刷新页面继续恢复同一 session；显式重新开始才创建新 session，并清空旧输入和提示。隔离 published 样板已完成桌面与 `390×844` 浏览器验收：精确标题自由输入推进至 revision 1，供应商不可用保持 revision 1，刷新恢复历史，重新开始回到 revision 0，移动端无横向溢出。该界面不等同于完整 AI 助教或 RAG 已上线。

## 13. 本地开发

后端：

```powershell
python -m venv .venv
& ".\.venv\Scripts\python.exe" -m pip install -r apps/api/requirements.txt
& ".\.venv\Scripts\python.exe" -m uvicorn apps.api.main:app --reload --host 127.0.0.1 --port 8000
```

后端请从仓库根目录启动。SQLite 与内容封存目录都按仓库根解析；在 Windows 中文路径下从 `apps/api` 子目录启动，可能触发 SQLite 写入失败。

前端：

```powershell
cd apps/web
npm ci
npm run dev
```

Vite 默认把 `/api` 与 `/ws` 代理到 `http://127.0.0.1:8000`；并行调试需要使用其他 API 端口时，在 `apps/web` 启动前设置 `VITE_API_PROXY_TARGET`，HTTP 与 WebSocket 会使用同一目标。

Windows 教师编辑器可在仓库根目录双击 `点我一键启动（部署）.cmd`。脚本首次运行创建 `.venv`、安装锁定依赖、为当前进程生成随机 `CHRONO_ADMIN_TOKEN`，再启动 API 与 Vite；API 依赖按 `requirements.txt` SHA-256 复用，文件未变化时不会重复联网安装。V0.9.13 起脚本不继承机器上的生产档：它先清除当前进程继承的 `CHRONO_*`，再设置进程级 `CHRONO_DISABLE_DOTENV=true`，显式固定 `local + debug + legacy-local + apply-safe + SQLite + mock`，只允许 `127.0.0.1` 前端 Origin 和 `127.0.0.1|localhost` Host，并固定把两个服务绑定到回环地址；`VITE_API_PROXY_TARGET` 同样重置为本地 API。`CHRONO_DISABLE_DOTENV` 只在导入 Settings 前从进程环境读取，不能写进 `.env` 后期待其关闭自身。脚本会修复 Windows 父进程中大小写重复的环境键，两个端口只占用其一时拒绝拼接半套服务；若后启动的服务失败，会回收本次已创建的进程树。根 `.cmd` 和脚本提示保持 ASCII 输出以规避 cmd 中文编码差异。手动启动时必须显式设置 `CHRONO_ADMIN_TOKEN`，未配置时所有管理员接口返回 `503`。

真实账号模式设置 `CHRONO_AUTH_MODE=accounts`。空数据库首次启动时还必须提供 `CHRONO_AUTH_BOOTSTRAP_USERNAME`、至少 12 字符的 `CHRONO_AUTH_BOOTSTRAP_PASSWORD` 和可选显示名；首管理员写入后再次启动不重复创建。浏览器生产部署应同时设置 `CHRONO_AUTH_COOKIE_SECURE=true`，并在 `CHRONO_CORS_ORIGINS` 中精确列出允许 Cookie 写请求的 Origin。后端内容 API 已支持账号 Cookie/Bearer 与角色矩阵，但当前教师编辑器尚无 accounts 登录页；一键编辑继续显式使用 `legacy-local`，不能把该启动方式当作生产部署。前端接入单列 FE-003，不影响 BE-003 后端任务结项。

V0.9.11 新增 `CHRONO_RUNTIME_PROFILE=local|production`，默认 `local`。`production` 会在创建 Engine 前执行跨字段门禁：必须关闭 debug、使用 `accounts`、启用 Secure Cookie、使用 `CHRONO_DATABASE_MIGRATION_MODE=validate`、提供非空 `CHRONO_DATABASE_URL`、清空共享 `CHRONO_ADMIN_TOKEN`，并把 CORS 配置为不含通配符、路径、凭据、回环地址的精确 HTTPS Origin；DeepSeek 模式还必须提供 API key。bootstrap 用户名与密码必须成对出现。V0.9.13 再要求 `CHRONO_TRUSTED_HOSTS` 为无重复、无通配符、无端口/协议/路径的小写精确主机名或 IP；production 拒绝 `localhost`、`testserver`、回环和未指定地址，`local + legacy-local` 只接受这些本机测试 Host。V0.9.14 进一步要求 production 数据库 URL 使用 `postgresql` 或 `postgresql+psycopg` 且包含 `sslmode=verify-full`；SQLite、其他驱动、仅加密但不校验证书主机名的 `sslmode=require` 和损坏 URL 都以稳定代码失败。V0.9.20 进一步要求 DeepSeek 地址使用 HTTPS、不得携带 URL 凭据/query/fragment，且主机必须位于 `CHRONO_DEEPSEEK_ALLOWED_HOSTS` 的小写精确非本地白名单；默认只信任 `api.deepseek.com`。同版本因遗留 Saga 状态和模型限流仍为进程内实现，要求 `CHRONO_API_WORKER_COUNT=1`；部署命令、进程管理器和该声明必须一致，当前也不得横向扩为多个副本。共享 Saga 状态和共享限流完成后才能解除此门禁。该门禁只验证配置声明，真实证书链、主机名握手和部署进程数仍必须由部署环境验收。校验异常不回显数据库、管理员或模型密钥。

`GET /healthz` 是无依赖存活检查，返回 `alive` 和应用版本。lifespan 在身份、内容与游戏服务初始化后一次性完成数据库/schema 与审计不变量只读基线复验；`GET /readyz` 随后只做轻量连接探测并返回已验证的 dialect 与当前/最新 schema 版本，避免公开探针反复扫描完整审计链。依赖不可用时返回 `503 runtime_dependency_unavailable`，不返回连接地址或底层异常。部署探针不能用 `/healthz` 替代流量就绪判断。

V0.9.12 的请求遥测接受 1—100 位 ASCII `X-Request-ID`，非法或缺失时生成 `req_<32 hex>`；同一 ID 写入 `request.state`、ContextVar、响应头以及既有认证/内容授权审计，CORS 显式暴露该响应头。每个请求只记录一条 JSON 消息：UTC 时间、请求 ID、HTTP 方法、FastAPI 路由模板、状态码、毫秒耗时和由连接地址/User-Agent 单向摘要得到的客户端标识；未匹配路由固定记为 `<unmatched>`。日志不读取原始 path、query、Authorization、Cookie、请求/响应正文、账号名或学生文本，且 lifespan 会关闭 Uvicorn 的重复访问日志。

V0.9.20 在路由解析、认证依赖和 Pydantic 读取之前增加全局请求体上限，默认 `CHRONO_API_MAX_REQUEST_BODY_BYTES=4194304`，可配置范围为 64 KiB—64 MiB。已声明超限的 `Content-Length` 零读取拒绝；无长度或 chunked 请求按实际收到字节累计，并在首次越界时停止继续读取。两种路径都返回 `413 request_body_too_large` 与 `Cache-Control: no-store`，外层请求遥测仍补充统一 `X-Request-ID`。该应用门禁不替代反向代理的连接数、速率、Header 和上传超时限制。

`production` 档的中间件会把尚未开始响应的未处理异常收束为固定 `500 internal_server_error`，JSON 事件只增加异常类名，不记录异常消息或 traceback；已经开始流式响应的异常仍向服务器上抛，避免伪造完整响应。本地 debug 保留原始异常抛出以便开发排查，因此本地故障演练不得使用真实凭据或学生原文。

V0.9.21 对 `POST /api/v1/auth/login` 与 `POST /api/v1/auth/token` 共用按 ASGI 连接端地址摘要区分的进程内令牌桶，不能通过切换 Cookie/Bearer 签发入口绕过；中间件不读取请求正文、用户名、密码或不受信的转发头。消费额度前必须只有一个 `application/json` Content-Type；其他媒体类型直接返回 `415 unsupported_media_type`。production Cookie 登录还必须先命中精确可信 Origin，否则返回 `403 csrf_origin_rejected`，因此跨站 `text/plain` / `no-cors` 请求不能借受害者或学校共享 NAT 地址消耗桶。默认每个客户端容量 10 次、60 秒线性补充，最多跟踪 10000 个客户端，可分别用 `CHRONO_AUTH_LOGIN_RATE_LIMIT_ATTEMPTS`、`CHRONO_AUTH_LOGIN_RATE_LIMIT_WINDOW_SECONDS` 和 `CHRONO_AUTH_LOGIN_RATE_LIMIT_MAX_CLIENTS` 调整。耗尽后返回 `429 auth_rate_limited`、`Retry-After`、`Cache-Control: no-store` 和统一 `X-Request-ID`，CORS 暴露关联头与重试头；其他接口不消耗该桶。合法 JSON 密码尝试仍按连接 IP 聚合，该实现只约束单 API 进程，不能冒充多 worker、多主机的共享防护；扩大部署前必须在可信网关或共享存储中建立全局限流，并明确可信代理来源。

本地数据库默认使用 `CHRONO_DATABASE_MIGRATION_MODE=apply-safe`，会在其他服务初始化前安全应用已登记迁移。`validate` 模式只接受已位于最新账本版本且真实结构一致的数据库，适合显式迁移后的启动门禁。数据库运维命令必须从仓库根目录、使用锁定虚拟环境执行：

API 数据库目标按以下优先级解析；URL 可能包含凭据，必须通过环境或未提交的 `.env` 注入，不得写入 Git、日志或命令回显：

```dotenv
# 可选；非空时覆盖 CHRONO_SQLITE_PATH。production 必须使用可校验证书主机名的 PostgreSQL。
CHRONO_DATABASE_URL=postgresql+psycopg://user:password@db.example.test:5432/chronovita?sslmode=verify-full
CHRONO_SQLITE_PATH=data/chronovita.db
CHRONO_DATABASE_POOL_SIZE=5
CHRONO_DATABASE_MAX_OVERFLOW=10
CHRONO_DATABASE_POOL_TIMEOUT_SECONDS=30
CHRONO_DATABASE_POOL_RECYCLE_SECONDS=1800
CHRONO_DATABASE_CONNECT_TIMEOUT_SECONDS=10
CHRONO_DATABASE_MIGRATION_LOCK_TIMEOUT_SECONDS=30
```

V0.9.14 的 PostgreSQL Engine 显式启用连接存活预检，并设置基础池容量、溢出、取连接等待、连接回收与建立连接超时；配置范围由 Settings 和底层 Engine 构造器共同校验。V0.9.18 进一步把 PostgreSQL 会话级迁移锁改为每 50ms 非阻塞探测，默认最多等待 30 秒，可通过 `CHRONO_DATABASE_MIGRATION_LOCK_TIMEOUT_SECONDS` 或迁移命令的 `--migration-lock-timeout-seconds` 在 0.1—300 秒内调整；超时以 `database_migration_busy` 失败关闭，不执行待迁移 DDL。`scripts/manage_database.py status|migrate` 可通过 `--database-url-env ENV_NAME` 从指定环境变量读取 PostgreSQL URL，URL 不进入命令行参数、标准输出或错误输出；空 PostgreSQL 数据库只有显式 `migrate ... --initialize` 才能创建 schema。`backup|verify-backup|restore` 仍只支持 SQLite 文件，学生账号/资产迁移也仍是 SQLite 离线命令。

生产 PostgreSQL 必须使用三个不同身份。引导身份由数据库平台或部署管理员持有，只负责创建角色和应用 schema；迁移身份是 schema 所有者，可执行登记 DDL，但没有超级用户、建库、临时表或角色管理权限；API 运行身份只有 schema `USAGE` 与逐表 ACL：迁移账本 `SELECT`，`kv/game_sessions/auth_users/auth_sessions` 为 `SELECT/INSERT/UPDATE`，`game_dossiers` 为 `SELECT/INSERT`，审计头为 `SELECT/UPDATE`，审计事件为 `SELECT/INSERT`。不授予表 `DELETE/TRUNCATE/REFERENCES/TRIGGER`、序列权限或面向未来表的默认权限；新增迁移必须显式声明并验证新表 ACL。部署先把迁移 URL 放入仅迁移步骤可见的任意环境变量，再以运行 URL 和 `validate` 模式启动 API；引导、迁移凭据不得进入 API 容器：

```powershell
$env:CHRONO_DATABASE_MIGRATION_URL = "postgresql+psycopg://migrator:***@db.example.test:5432/chronovita?sslmode=verify-full&options=-c%20search_path%3Dchronovita"
& ".\.venv\Scripts\python.exe" scripts\manage_database.py migrate --database-url-env CHRONO_DATABASE_MIGRATION_URL

$env:CHRONO_DATABASE_URL = "postgresql+psycopg://runtime:***@db.example.test:5432/chronovita?sslmode=verify-full&options=-c%20search_path%3Dchronovita"
$env:CHRONO_DATABASE_MIGRATION_MODE = "validate"
```

`.github/workflows/postgres-service.yml` 在独立 PostgreSQL 16.14 服务中先运行 `scripts/provision_postgres_test.py`：只有该步骤收到一次性管理员 URL，并把随机口令的非超级用户迁移/运行 URL 写入 GitHub 管理的环境文件；`--github-env` 在 GitHub Actions 之外会失败关闭。后续测试进程显式拒绝管理员 URL。双 Engine 并发迁移、连续账本、真实反射、环境变量式 CLI 和迁移锁恢复只使用迁移角色；迁移后由表所有者授予精确运行 ACL，accounts ready、账号、跨实例审计、管理员不变量、KV 并发、学习进度和游戏会话只使用运行角色。矩阵要求运行角色建表、建 schema、改表、删表、截断、切换迁移角色、改迁移账本、修改/删除审计事件、删除 KV 和改写卷宗全部返回 PostgreSQL `42501`，且每条反例无条件回滚并重置角色。`.github/workflows/backend-quality.yml` 在 Python 3.11 上安装显式固定的 API 直接依赖和固定版本 `pip-audit`，依次执行 `pip check`、`scripts/audit_python_dependencies.py`、后端编译、仓库内文档契约校验和全量 `unittest`；审计脚本强制子进程使用 UTF-8，使 Windows 中文仓库路径与 CI 使用同一入口。两条工作流都对 `houduan` 与 `main` 推送生效，失败会使对应 Check 失败；当前仓库尚未启用 branch protection 或 ruleset，因此这些 Check 还不是不可绕过的合并门禁。工作流把有界测试尾部写入任务摘要，并在失败时生成标准错误注释，使公开 Check API 无需下载完整日志也能定位失败。`infra/compose.postgres-test.yml` 提供同版本、无持久卷、仅绑定回环地址的本地测试服务。固定引导账号和密码仅属于一次性测试数据库，迁移/运行口令在独立引导进程内随机生成且不输出；这些凭据均不得复制到生产。该矩阵使用无 TLS 的 CI 内部服务，不证明生产证书、共享限流、故障转移、`pg_dump` 或时间点恢复。

V0.9.23 实现提交 `ba36afe` 已通过 [PostgreSQL 16 最小权限服务门禁 30125906764](https://github.com/falling-feather/Chronovita/actions/runs/30125906764) 与 [后端质量门禁 30125906802](https://github.com/falling-feather/Chronovita/actions/runs/30125906802)。前者在真实容器中执行迁移/运行角色正反矩阵，后者执行依赖一致性、已知漏洞审计、编译、文档契约和 293 项后端测试。

`provision_postgres_test.py` 会删除并重建固定测试 schema 与角色，因而只接受回环地址上的 `chronovita_test` 数据库和 `chronovita_test_admin` 账号；其他主机、库名或账号在连接前即被拒绝。它不是生产角色管理工具，不得放宽该保护后用于正式数据库。

```powershell
# 在本地一次性测试库中准备随机角色，并在不向子进程传递管理员 URL 的前提下运行真实服务矩阵
$env:CHRONO_TEST_POSTGRES_ADMIN_URL = "postgresql+psycopg://chronovita_test_admin:***@127.0.0.1:55432/chronovita_test"
& ".\.venv\Scripts\python.exe" scripts\provision_postgres_test.py --run-tests
Remove-Item Env:CHRONO_TEST_POSTGRES_ADMIN_URL

# 只读查看当前 schema；不会创建或迁移数据库
& ".\.venv\Scripts\python.exe" scripts\manage_database.py status --database data\chronovita.db

# 先创建一致快照；默认清单为 chronovita-before-migrate.db.manifest.json
& ".\.venv\Scripts\python.exe" scripts\manage_database.py backup --database data\chronovita.db --output backups\chronovita-before-migrate.db
& ".\.venv\Scripts\python.exe" scripts\manage_database.py verify-backup --backup backups\chronovita-before-migrate.db

# 显式应用已登记安全迁移；新文件必须额外传 --initialize
& ".\.venv\Scripts\python.exe" scripts\manage_database.py migrate --database data\chronovita.db

# PostgreSQL URL 只从命名环境变量读取；空库初始化需要 --initialize
$env:CHRONO_MIGRATION_DATABASE_URL = "postgresql+psycopg://user:password@db.example.test:5432/chronovita?sslmode=verify-full"
& ".\.venv\Scripts\python.exe" scripts\manage_database.py status --database-url-env CHRONO_MIGRATION_DATABASE_URL
& ".\.venv\Scripts\python.exe" scripts\manage_database.py migrate --database-url-env CHRONO_MIGRATION_DATABASE_URL --migration-lock-timeout-seconds 30
```

`backup` 对有效 V0.8.x 旧库也可执行，因此升级顺序固定为“先备份并验证，再迁移”。它通过 SQLite backup API 读取包括 WAL 中已提交内容的一致视图，把快照规范为独立文件，再记录 `chronovita-backup-manifest/v1`：仅包含 backup ID、UTC 时间、应用/schema 版本、是否存在账本、备份文件名、字节数、SHA-256 和 `quick_check=ok`，不包含源路径、数据库 URL、密码、token 或业务正文。备份文件和 manifest 均只创建不覆盖；两份临时文件各自完成文件级 `fsync` 后，先发布数据库硬链接并同步数据库目录，再发布 manifest 并同步其目录，因此 manifest 是最后的完成标记，崩溃留下的无 manifest 数据库不会被验证为可用备份。Windows 使用带 `FILE_FLAG_BACKUP_SEMANTICS` 的目录句柄调用 `FlushFileBuffers`，POSIX 使用目录描述符 `fsync`；目录同步失败会删除本次发布的目录项并再次同步，不能把“函数返回成功但断电后目录项丢失”冒充成耐久备份。路径身份权限错误不会降级成“不相同”；主故障与临时文件清理故障同时发生时保留主异常并附加清理说明。

恢复前必须停止 API 和所有数据库写入进程。默认只能恢复到不存在的目标：

```powershell
& ".\.venv\Scripts\python.exe" scripts\manage_database.py restore --backup backups\chronovita-before-migrate.db --target data\chronovita-restored.db
```

覆盖现有目标必须显式传 `--replace`；工具会先为旧目标生成并验证带 manifest 的 `*.pre-restore-*.db` 安全副本，也可用 `--safety-backup` 指定新路径。恢复在临时文件上先复验 manifest、checksum、`quick_check` 和 schema，再同目录链接或替换目标并同步父目录，最后重开复验；目录同步或末次复验失败时，既有目标会从安全副本替换、同步并复验，不存在的目标会被删除并同步目录。目标旁存在 `-wal/-shm/-journal` 时拒绝覆盖，需先确认 API 已完全停止并正常 checkpoint；不得手工删除仍在使用的 sidecar。所有命令成功和失败均输出 ASCII JSON，且不打印数据库 URL 或业务数据。

从单用户库切换 accounts 前先停止 API 并备份数据库。目标账号必须已经存在、启用且包含 `student` 角色，不能把纯管理员当作临时学生。若首次 accounts 启动创建 bootstrap 管理员后因旧资产被门禁拒绝，保持 API 停止，先用下列离线命令创建学生账号；命令会分别安全提示管理员密码和新学生密码，不把口令放入命令行或输出：

```powershell
& ".\.venv\Scripts\python.exe" scripts\create_student_account.py --database data\chronovita.db --actor-username root.admin --username student.one --display-name "Student One"
```

随后先运行不带 `--apply` 的计划，核对数量、来源用户和目标账号，再使用完全相同的参数应用：

```powershell
& ".\.venv\Scripts\python.exe" scripts\migrate_student_assets.py --database data\chronovita.db --actor-username root.admin --target-username student.one --source-game-user-id local-student
& ".\.venv\Scripts\python.exe" scripts\migrate_student_assets.py --database data\chronovita.db --actor-username root.admin --target-username student.one --source-game-user-id local-student --apply
```

`--source-game-user-id` 必须填写旧库创建数据时实际使用的 `CHRONO_GAME_USER_ID`，不是目标账号名。两个命令默认从交互终端读取口令；无交互自动化可分别临时设置 `CHRONO_MIGRATION_ACTOR_PASSWORD` 与 `CHRONO_NEW_STUDENT_PASSWORD`，命令结束后必须在调用方环境立即清除，脚本不会打印口令。dry-run 不写学生资产或迁移回执，但真实登录/退出仍会留下认证审计；apply 把资产、迁移回执和 `auth.student_assets.migrate` 审计作为同一事务提交。执行期间不得同时运行 API 或其他数据库写入进程。任何 `student_asset_migration_*` 错误都必须先保留数据库副本并排查，不要手改回执或 JSON checksum。

## 14. 测试与质量基线

- 后端当前使用标准库 `unittest`，统一从项目虚拟环境执行：`& ".\.venv\Scripts\python.exe" -m unittest discover -s tests -v`。依赖安全复验执行 `& ".\.venv\Scripts\python.exe" scripts\audit_python_dependencies.py`；脚本要求虚拟环境已安装质量工作流固定的 `pip-audit==2.10.1`。不要使用机器全局 Python 生成或核对契约 Schema。
- 身份专项测试覆盖随机盐 scrypt、空库显式首管理员、统一登录失败、Cookie/Bearer、明文令牌不落库、退出、重复账号、动作权限、角色/禁用/密码变更撤销、最后管理员保护、legacy 冲突凭据、审计链篡改检测，以及审计故障时会话和用户写入的事务回滚；SQLite 与 PostgreSQL 服务矩阵另覆盖多 Engine 单一 bootstrap、两管理员并发降权只提交一项、多 Engine 审计追加与并发复验。
- 内容 RBAC 专项逐路由核对 `content.read|author|review|publish`，覆盖学生拒绝、教师草稿所有权、双角色自审拒绝、审校者不能写稿或发布、管理员封存发布、旧 `actor/sealed_by` 防伪、直接制品重签、Cookie Origin、审计故障零写入，以及两个教师并发首建时只产生一个作者。
- 游戏所有权专项在 accounts 模式创建两个学生与一个教师，覆盖客户端 `user_id` 防伪、同一启动请求按账号隔离、Cookie 写入可信 Origin、会话/行动/自由输入/卷宗/回放的跨用户 `404 resource_not_found`、未知资源同语义、教师摘要独立权限、读取审计及审计故障 `503`，以及 legacy 会话不被首个真实账号自动接管。
- 遗留 Practice 安全专项覆盖 Saga/Ask/Canvas/LLM 信息的匿名与非学生拒绝、跨用户统一 404、所有者字段不回显、用户和全局 Saga 容量、终局保留、TTL、动作租约、模型令牌桶与并发槽、输入和输出边界、Canvas 超长响应、畸形 META、服务端七回合终止、供应商异常回滚，以及响应头前与首个正文块后的真实 ASGI 断连清理；production 配置专项另验证多 worker 声明、DeepSeek 明文/非白名单/带凭据端点失败关闭，请求体专项验证声明长度零读取拒绝、chunked 首次越界停止和认证依赖零调用。
- 学习资产所有权专项在 accounts 模式创建两个学生，覆盖同课时进度与画板独立存储、未认证拒绝、旧 `default:*`/裸键不被真实账号读取、带下划线用户 ID 的前缀转义、伪造复合 lesson ID 拒绝，以及一次确定性 CAS 竞争后的进度层级合并；切回 `legacy-local` 后只读取原单用户键。
- 学生资产迁移专项覆盖真实管理员登录与失效会话拒绝、dry-run 不改学生资产、进度/旧画板/V1 与 V2 会话/final 卷宗同事务映射、release identity 与会话/卷宗 ID 保持、checksum 重签、迁移审计和回执幂等重跑、纯管理员/禁用目标与已知非学生所有者拒绝、目标键冲突零覆盖、回执及审计末端故障整事务回滚、孤立卷宗、错误旧用户、未知账号键、结构化 CLI 错误，两个离线命令对未迁移库在读密码前失败，以及空身份旧库从首次启动阻断、离线建学生账号、dry-run、apply 到 accounts 重启成功的完整路径。
- 数据库 schema 专项覆盖空库幂等建库、`validate` 零写入、V0.8.x `kv + game` 与 V0.9.5 身份库原样认领、半套/未知/漂移表拒绝、账本 checksum 改写/缺号/超前拒绝、审计头篡改、迁移中途失败整事务回滚、双 Engine 并发迁移单一连续账本、应用 `init_engine()` 确实经过统一管理器，以及两个 Store 构造器在空库上零建表/零初始化；已发布迁移契约 checksum 由测试固定，不能原地改写。
- 数据库备份恢复专项覆盖当前库和 V0.8.x 旧库往返、活动 WAL 一致快照、schema/审计复验期间的 WAL 并发写、manifest/文件 SHA-256/大小/`quick_check`/schema 联合验证、重复 JSON key、文件与清单篡改、未来 schema、目标和硬链接别名冲突、SQLite sidecar、备份与清单只创建不覆盖、manifest 最后提交顺序、目录同步故障清理、路径身份检查失败、主故障与清理故障组合、无关外层异常隔离、覆盖前安全副本、恢复末次验证失败自动回滚，以及 `status/migrate/backup/verify-backup/restore` 结构化 CLI 整链。
- 数据库配置专项覆盖 SQLite 兼容路径与 URL 优先级、仓库根相对路径、显式内存库跨连接一致、同进程目标冲突、只接受同步 SQLite/Psycopg 驱动、默认 Psycopg 3、驱动缺失脱敏、Settings 秘密值遮蔽，以及全部八张登记表的 Postgres DDL 编译；未连接真实 Postgres 服务。
- 内容发布测试覆盖全状态流转、无效与陈旧草稿、并发保存指纹一致性、审批/封存并发指纹、封存/关卡暂存不可见、三态关卡绑定、显式旧内容迁移、V1 现场升级、课程/游戏同 release、同课程多课时投影、严格 ID 路径校验、跨课程课时/关卡 ID 冲突、整体回滚、历史制品复验、服务重载、可信审计身份、路径身份与 checksum 篡改；并故障注入首发、并发发布、工作流、active pointer 与进程中断，验证事务恢复、线上 release 不移动、孤立 manifest 不进入历史且损坏或缺失制品稳定失败关闭。
- 历史局势测试从两份 sealed 文件加载无节点危机治理与节点式制度改革关卡，覆盖重复重放、在线/批量一致性、状态 set/add 与限幅、NPC 条件和效果、事件、四类可达结局、幂等原始 revision、终态、无行动、回合上限、派生产物、嵌套会话、checksum、重复 key、超大文件与符号链接篡改；API 另覆盖 development/published 双来源目录、V2 历史完整 release pin、缺失或部分 pin、有效但不可达清单、旧 pin 跨发布、同产物跨 release 幂等隔离、真实 V1 课程投影、旧会话跨发布/回滚、身份漂移、课程复用、稳定 404/409/422/503、固定/自由输入字段隔离、禁止客户端状态、并发单写、返回对象隔离、OpenAPI 响应和 legacy sandbox 兼容。SQLite 持久化测试再覆盖完整应用重启续玩、原始 V1 checksum 兼容读取、V1 伪装新证据拒绝、成功 CAS 原子升级 V2 并保留 release identity、两个独立连接的陈旧 CAS、跨服务同动作幂等、自由输入同 key 不同分类结果的唯一赢家、不同自由输入争抢同 revision 的一胜一冲突、分类期间 revision 漂移、零写入分类结果、幂等恢复仍复验完成态卷宗、原子完成态封卷、卷宗插入失败回滚、显式旧会话补建、离线精确回放、教师摘要、外层 checksum 篡改、重算 checksum 后的 basis/output/history/summary/卷宗 consequence 伪造、固定场景版本消失和存储表故障；严格 LLM 适配测试覆盖端到端 deadline、限流、4xx/5xx、超长、截断、畸形信封、重复 key、非有限数、严格输出 Schema 和 legacy 流式脱敏回退；行动归类测试再覆盖精确别名免模型、当前行动白名单、已审 facts 最小上下文、低置信、史实边界、提示注入、输入边界、未知模型行动、伪造模型实例/完成信封、矛盾结果合同与供应商失败不改会话；叙事测试覆盖封闭输出 Schema、上下文脱敏、待审 facts 隔离、引用越界、超时回退、规则 basis、单次 CAS、幂等重试、叙事期间 revision 漂移与离线 replay。Canvas 测试另覆盖缺失与显式空画板区分、旧信封迁移、稳定错误码、损坏数据失败关闭、陈旧 PUT 不覆盖和同一快照并发唯一胜者。运行基线另覆盖生产配置反例、秘密不回显、数据库/schema 就绪失败、liveness 独立、请求 ID 关联、非法 ID 替换、路由模板日志、敏感字段缺席、生产异常脱敏、精确 Host、登录令牌桶、请求体上限、可信模型端点、429 关联头、有界客户端状态、单 worker 声明和进程级 dotenv 禁用选择。当前后端全量发现 272 项，其中 271 项通过、1 项真实 PostgreSQL 服务测试在未配置服务的本地/通用质量工作流中跳过。
- `tests/test_dayu_end_to_end.py` 使用隔离内容根目录、SQLite 与离线 `mock` 串联真实 HTTP 边界：教师课程保存、校验、送审、批准和封存，低代码关卡保存、校验和封存，V2 联合发布，公开课程生成完整 pin，缺 pin 启动拒绝，语义输入因旧内容无逐条资料绑定而零写入，精确别名自由输入和固定行动进入四回合终局，final 卷宗与 checksum、离线 replay、Canvas CAS 及应用重启后的同会话/同卷宗/同画板恢复。它是 AI-001 与 FE-002 的阶段收口证据，不等于已完成 QA-001 的生产矩阵。
- V1 契约修改后必须先重新运行 `scripts/export_runtime_contracts.py`，再执行测试，防止提交的 JSON Schema 与 Pydantic 模型漂移。
- saga 压力测试脚本见 `scripts/test_saga_batch.py` 与 `scripts/test_saga_stress.py`。
- 前端上线前至少跑 TypeScript 检查与关键路径浏览器烟雾测试。
- 关卡化交互上线前要验证：状态变化、选择收束、异常回退、移动端布局。
- 学生自由行动当前浏览器基线覆盖：published 课时 release pin、精确自由输入推进、零写入供应商回退、刷新恢复、重新开始状态清理、桌面及 `390×844` 响应式布局；控制台只保留既有 React Router v7 迁移提示。前端生产构建通过，Vite 仍有既有主包体积提示。
- 卷宗到画板当前浏览器基线覆盖：隔离 published 大禹样板三回合完成后从“练”层直接进入“创”层，读取同一 session 的 final 卷宗，保留 1 个学生原节点并导入 2 个知识节点；自动保存后刷新仍为 3 个节点且导入按钮保持幂等禁用。两个页面从同一 revision 同时新增节点时，第一页保存成功、第二页稳定进入 `409` 冲突锁定，服务器只保留胜者；第二页载入最新版本后恢复编辑且不携带败者节点。桌面与 `390×844` 首次加载均无页面横向溢出，手机画板会把 3 个节点完整 fit 到可视范围；控制台无 React Flow 或应用错误，只保留既有 React Router v7 迁移提示。前端生产构建通过，`LessonCreate` 保持独立懒加载 chunk，Vite 仍有既有主包体积提示。

## 15. 不可过度承诺

- accounts 身份、内容 RBAC、学生资产隔离、旧数据映射、显式数据库生命周期、生产配置与健康门禁、请求追踪、脱敏 JSON 访问事件、单进程认证限流、全局请求体上限、可信 Host/模型端点和一键本地模式已经建立；跨实例共享限流、真实 Postgres 服务及原生备份、发布门禁、前端 accounts 登录仍未收口，整体不能宣称生产部署完成。
- 尚未有 RAG 引证闭环。
- 尚未有教师后台和内容 CMS。
- 尚未有班级学情聚合、失败路径统计与教师分析看板。
- 真实视频/音频生成管线仍在规划。
